# Docker Build
docker buildx build --platform linux/amd64 --no-cache -t patsiesoblast0l/option-combo-starter:20260408 .

# Docker Run (sample with all available env overrides)
docker run -d \
    --name option-combo \
    -p 8000:8000 \
    -p 8765:8765 \
    -e TWS_HOST=10.3.10.253 \
    -e TWS_PORT=7496 \
    -e TWS_CLIENT_ID=999 \
    -e WS_HOST=0.0.0.0 \
    -e WS_PORT=8765 \
    patsiesoblast0l/option-combo-starter:20260408
