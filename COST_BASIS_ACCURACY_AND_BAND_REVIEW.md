# COST_BASIS 全功能复核、压力测试精度与校准带评审

> 历史基线审计：以下定位与问题描述针对重构前的 `dafd789`，不是当前待修复清单。
> 后续已按用户授权替换压力测试内核；现行语义与保留的模型限制见 `COST_BASIS_LONG_PUT_STRESS_REVIEW.md`。
> 原探针入口已迁移为新内核的离线不变量审计。原方案已改为非概率的采样情景范围。

评审日期：2026-09-05。代码基线：`dafd789`。方案：`CODE PLAN/STRESS_CALIBRATION_BAND_PLAN.md`（待实施）。

本轮只做审查与离线验证，未修改生产逻辑、真实账本、行情订阅或交易状态。本文是新的评审结论，不延续旧 Review 的逐轮修复日志；旧维护说明中“未发现已知生产估值问题”的结论不再覆盖本轮新发现。

## 1. 总结

账本、导入、对账、持久化的主线较扎实；压力测试的分项、逐合约 IV、美式估值和缺数据停算机制也值得保留。但不能据此认为“模型参数已经收敛到数据支持的精度”。

本轮发现了现金守恒、到期日和纯期权账本三个常用边界的问题，另有零价格的美式估值错误、非 USD 输入缺少隔离。模型层面，**一日杠杆收益映射的依据写错了**；当前模拟还缺少今日报价重定价一致性和整条预测链的样本外验证。

校准带**工程可行，但不建议按现稿直接落地**：它目前最多是“选定参数场景的敏感性包络”，不是已识别的参数不确定区间，更不是未来盈亏预测区间。八个角加中线也不能保证找到连续参数区间内的极值。应先修基础边界，再明确带子的统计含义与计算保证。

## 2. 全页面扫描范围与结论

| 功能链 | 核对内容 | 本轮判断 |
| --- | --- | --- |
| 账本与成本 | 事件重放、现金/数量符号、净现金/股票/税务口径、Long/Short 权利金分桶 | 保留“Long 全周期权利金不污染标的综合成本”的设计；但压测不能直接把这个成本展示值当成完整资产盈亏 |
| CSV 与历史补齐 | Activity/Flex、成交/交割配对、期初推导、累计报表去重、TWS 临时基线替代 | 现有回归含真实报表 fixture，未新增可复现的导入问题 |
| TWS 对账与成交 | 账户/标的作用域、execId、显式采信、旧请求失效 | 未发现本轮范围内的自动记账或下单越界；账本缺失的保护仓位仍不会自动进入压测 |
| 流水与分页 | 完整事件集用于重放、流水筛选与总览隔离、切换账本代次 | 主线正确；超大账本截断仍可能只提示一次后展示不完整总览，宜持续显示“数据未完整载入”并阻止压测 |
| 存储与危险操作 | 批量导入原子性、void 后时间线校验、重建先存档、整本删除确认 | 现有保护较完整；未进行真实删除/重建操作 |
| What If | 单价结算、交割现金仅计股票交付、不重复计权利金 | 适合作为静态结算/成本展示；多个到期日被按同一个假设价结算，不等于真实路径回放 |
| 压力测试 | 本账本长短期权、联动 Long C/P、IV 开关、期限/价外折扣、路径 σ、每周收入 | 存在下节问题；合计目前不是统一基准的账户净值变化 |
| 行情/后端 | 一次性有界取数、逐批清理、mark/IV/BBO、共享利率曲线 | 缺项有拦截；但“同批取到”不等于“报价时间一致”，也不等于模型与中间价一致 |
| 产品边界 | STK/OPT 与 FUT/FOP、币种、备份 | FUT 压测明确不支持，不是遗漏实现；非 USD 压测应限制；`cost_basis.db` 不在 workspace 自动恢复集内 |

检查涉及 `cost_basis.html`、`js/cost_basis*.js`、`js/american_binomial.js`、`cost_basis_store.py`、`cost_basis_ws.py`、`cost_basis_executions.py`、`ib_server.py` 的账本行情路径、`ib_server_market_data.py`、研究脚本及相关测试；也对照了共享定价/时钟模块的实现。这里的“未发现”是本次功能链扫描和现有测试结论，不是穷尽证明。

## 3. 已确认的实现问题

### F1 · P1：本账本 Long Option 到期后，已付权利金会从压测盈亏中消失

定位：`js/cost_basis.js:1557` 的结算与 `basePnl`，`js/cost_basis.js:1610` 附近的 deferred 估值；`js/cost_basis_core.js:1446`、`:1580`。

`_attachCostLenses` 有意从标的成本排除 Long 全周期权利金。未到期时，独立 Long 分项通过 `marketValue + openPremium` 补回；到期后，该合约不再属于 `deferredOptions`，独立分项归零。股票交割价值进入了结算项，但 Long 的支出没有补回。

离线例子：200 股 × $100，另买 1 张 K100 Put、支付 $500；场景股价 $80，无息无股息，开启本账本期权估值。

| 情景日 | 当前输出 | 按现金与持仓核算 |
| --- | ---: | ---: |
| 到期前一天 | −$2,500 | −$2,500 |
| 到期日，行权交付 100 股后仍余 100 股 | −$2,000 | −$2,500 |

到期日正确值为 `剩余股票 8,000 + 交付收款 10,000 − 买股 20,000 − 买 Put 500 = −2,500`。差额恰好是已付权利金，不是 theta、IV 或二叉树误差。OTM 到期也需要保留这笔损失。

建议：保留账本成本口径，单独定义压测纳入的期权现金/资产贡献，在 deferred → settled 转换时守恒。不能直接改综合成本把所有 Long 费用塞回，也不能再加一次已通过股票交割体现的 intrinsic。验收覆盖 Long C/P、ITM/OTM、部分交割、交割后正/零/负股数及各成本视图。

### F2 · P1：联动仓位在到期当天被整天忽略，且“今天”来自浏览器时区

定位：`js/cost_basis.js:279`、`:1167`、`:3323`、`:4569` 附近。

`estimateLinkedLongOptions` 用 `expiryAt <= asOfAt` 丢弃合约；联动请求又只选 `expiry > _todayDigits()`，导致今天到期的 Long C/P 连 mark 都不请求。二者共同把仍可交易/行权的保护仓位视为不存在。

同一张到期日 K100 Put、mark $5、情景价 $80：`asOf=到期前一天` 时保护增值为 $1,500；`asOf=到期当天` 时返回 `available=true, count=0, pnl=0`。这不是显式“不可用”，而是一条少算仓位但看似有效的曲线。

此外 `_todayIso()` 用本机日期。上海凌晨仍可能是纽约前一交易日下午，问题会提前触发。主账本在同日却会按 intrinsic 结算，两本账本的到期边界也不一致。

建议：明确 `quoteAsOf / scenarioAsOf / expiryCutoff`，使用合约/交易所时区与截止时间；当前仍开放的到期日仓位必须保留。先明确“即时情景”与“当天收盘结算”的区别，不能只把 `<=` 换成 `<` 就认为时间问题全部解决。共享 `pricing_context.js`、`pricing_core.js` 已有相关时钟能力，应抽取/复用纯逻辑，不加载交易 shell。[OIC 对美式期权行权日的说明](https://www.optionseducation.org/videolibrary/what-is-the-difference-between-american-style-and)也明确包括到期当天。

### F3 · P2：没有股票、但仍有已成功估值的期权时，整个压测不可用

定位：`js/cost_basis.js:1562–1573`、`:1724`、`:1742`。

股票为零时，每股成本是 null，`basePnl` 随之为 null。兜底只允许“没有任何未平期权”；即使后面的 Long/Short 估值完整，合计也无法恢复为数值。

例子：只保留 F1 的 1 张 Put，到期前一天股价 $80。Long 分项已算出 $1,500，但 `available=false, reason='', headlinePnl=null`。纯期权账本及股票被全部交割、仍留远期保护的组合都会遇到此边界。

建议：把“无每股成本”和“无组合价值”分开；用独立现金/持仓基础项聚合。不能无条件以 `lifetimeNetCash` 兜底再加含权利金的期权 P&L，否则会重复计入现金。

### F4 · P2：零标的价时，美式 Put 意外退回欧式折现价值

定位：`js/cost_basis.js:482–483`。

`spot <= 0` 与 `timeYears <= 0` 共用 BSM 分支。K100、剩余一年、r=5%、S=0 的美式 Put 输出 $95.12294，低于立即行权的 $100。美式定价器内部的 intrinsic 下限被绕开了。

标准 TQQQ 30% 扫描通常碰不到；但自定义线性联动倍数可以把联动价格截到零，定价函数也明确接受该边界。应分别处理到期、零价、无效负价；负利率情形另做定义和测试，不把所有 r 都硬编码成 K。

### F5 · P2：非 USD 账本可进入 USD 曲线估值，行情标的筛选也没有完整币种约束

定位：`cost_basis_store.py:1447`、`ib_server.py:2490–2520`、`:2580` 附近和 `:2611` 的利率解析；`js/cost_basis.js:3991`。

账本允许一般三字母币种；压测后端没有拒绝非 USD，却统一读取共享 USD 曲线。两本账本“币种相同”的检查不能证明折现曲线币种正确。持仓中借用 STK 合约时只比较账户和 symbol，没有同时比 currency；同名跨市场持仓可能借错标的价格。

这是静态路径确认，未连接非 USD TWS 实盘复现；不影响身份正常的 TQQQ/QQQ USD 常规路径。近期最小修正是只开放 USD 压测并验证实际合约币种；真正支持其它币种需要对应利率曲线及产品语义，不应只换货币符号。

## 4. 精度上更重要的模型问题

### 4.1 一日杠杆映射的解释与实现边界需要纠正

`STRESS_MODEL_RESEARCH_MEMO.md` §2.1 把“一日内 `(1+ΔT)=(1+R)^3`”称为机械事实，`js/cost_basis.js:1037` 附近注释也据此否定 −30% → −10%。这是错误的。

理想每日重置、以该日重置点为基准、忽略费用和跟踪误差时，`r_L,day = k × r_index,day`；多日则是：

```text
ETF gross return   = ∏(1 + k × r_day)
Index gross return = ∏(1 + r_day)
```

单日 ETF −30%、k=3，基准指数应为 −10%；当前 `compound` 在 T=0 给出约 −11.21%，T=1/365、σ=30% 给出约 −11.19%。本轮离线检查得到指数价 $88.8123，而理想日目标是 $90。该公式不是每日 ETF 的精确恒等式。[ProShares 的 TQQQ 目标及多日偏离说明](https://www.proshares.com/our-etfs/leveraged-and-inverse/tqqq)支持的是**每日收益三倍**，不是价格比三次方；QQQ 本身也只是 NDX 的可交易代理，应另计跟踪差异。

现有 `k·log-return − (k²−k)σ²T/2` 可作为连续再平衡、小日收益、给定累计方差下的近似，但不能在单日大跳跌时仍称“更精确”。同一个多日 ETF 终值也不能唯一反推 QQQ 终值，除非额外给定路径/方差假设。

建议：即时/单重置区间建立正确边界；多日先用 QQQ 日路径正向生成杠杆 ETF，再用实际 TQQQ/QQQ 对齐日线校准费用、分红、拆股和跟踪误差。若图仍以 TQQQ 跌幅为横轴，多路径结果需做条件分组/求解，不应假装逆映射唯一。

### 4.2 今日基准没有收敛检查，模型差异会被当成保护增值

后端 mark 优先用 BBO 中间价，IV 优先用 `modelGreeks`，随后还可能用 bid/ask/last IV（`ib_server_market_data.py:415`、`:568`）。前端直接重定价，再减今日 mark；未验证“同现价、同一时刻、无冲击”时是否重现今日市值。

审计脚本故意输入不一致的 mark=$5、IV=30%、一年期 ATM Put：没有价格变动、没有时间经过、没有 IV 冲击，联动 P&L 仍是 +$694.78/张且 `available=true`。这是证明缺少一致性检查的合成例子，**不是本轮观测到的真实 TWS 偏差**。

研究备忘 §2.10 的“TWS 美式模型与本页 CRR 一致”没有被当前研究证明。同为美式也不保证分红、期限、树步数和目标价格一致。[IB 官方 tick 类型文档](https://interactivebrokers.github.io/tws-api/tick_types.html)区分 bid、ask 和 model 价格对应的 IV，不能把它们视为同一个中间价 IV。

建议优先增加：

- 冻结同一个快照，保存期权与标的各自的报价时间、来源、质量、合约截止时刻；`fetchedAt` 只是请求完成时间。
- 按所选模型、股息和时钟，从有效中间价反解本地 IV；重定价误差以 tick/价差/绝对金额设门槛。不能反解的深 ITM、缺外在价值等情况要明确降级或停止，不伪造 IV。
- 将模型残差独立显示；若采用 `市场价 + 模型变化量` 的过渡方式，需处理 intrinsic/无套利边界及残差到期归零，不能永远保留一个常数偏移。
- 两本账本、所有参数成员共用同一冻结快照。联动账本刷新不一致、逐批数秒价差不能变成新的“参数不确定性”。

### 4.3 “统一情景日”已实现，但跨多个到期日的路径没有实现

`computeOptionSettlementScenario` 与联动 `settled` 分支，把情景日前各张到期合约都按**最终情景价**结算。若 Put 在第 5 天到期、第 20 天才跌到目标价，它不会在第 5 天获得第 20 天才出现的 intrinsic；相反，先跌后反弹又可能漏掉已实现的保护。

这对“所有期权今天同时按假设价结算”的静态图可以成立，但对“几天跌到位”的未来预测不足。最低成本改进是先提供三种确定性路径：立即跌到位、逐日下跌、先跌后反弹；在各自到期日结算并持有相应现金/交付股票到统一终点。资金利息、股票分红和提前指派风险也随路径处理。

不必第一步就上复杂随机波动率模型。美式树给出了行权权利的价值，不代表系统已经模拟提前指派后的持仓、保证金和融资需求。

### 4.4 IV 经验规则是可用的基线，还不是联合验证过的预测器

目前的组合是 `当前 IV + β(drop) × tenor(p) × OTM折扣`，本账本再乘 `|ratio|`。代码乘法/开关已有测试，但各部分分别“有研究依据”不等于相乘后仍能预测正确。

需要补的验证包括：

1. **持有期不能轻率合并。** 仓库 A1 报告中 10–20% 跌幅的 β 从 5 日 1.53 到 40 日 0.91；1 日 5–10% 档是 1.64、40 日是 0.87，部分档样本很少。这不足以证明显著差异，却更不足以宣布持有天数几乎无影响。UI 的 0/1 天情景不能仅凭 5–40 日合并回归认为已校准。
2. **ATM30 不是严格恒定期限。** 脚本选最近到期日；缺 IV 后先删行再按下标取 h 天，h 可能跨缺失交易日。应保留交易日骨架，在到期日间以一致的总方差方法构造 30 日锚点，标注外推/缺失。
3. **期限变量不完全一致。** `skew_regime_study.py:96–168` 拟合使用事件起始 `dte0/front_dte`；运行时 `js/cost_basis.js:596` 附近衰减用目标日剩余 DTE。且研究的平均固定行权价抬升与 β 的滚动 ATM30 抬升不是同一个量。应在联合回放中统一起始期限、剩余期限、冲击持续天数和 moneyness 定义，而非再次只修改指数。
4. **适用对象有限。** QQQ OTM Put、七次下跌和五次反弹的数据，不能直接证明 TQQQ IV 冲击永远为 3 倍，也不能证明 ITM Put/Call 上涨侧固定 IV 正确。原备忘也承认缺 TQQQ 历史链。
5. **微笑研究是事后拟合。** sticky-strike 与 sticky-delta 的比较给每个事件/到期日都拟合了事后平行位移，22/34 是样本内形状比较，不是完整模型的预测胜率。可优先保留 sticky-strike，但不宜写成另一类模型已被永久否决。
6. **存续样本与样本依赖。** 多到期日来自同一事件，5/10/20/40 日窗口相互重叠；33 条或 25 条不是同等数量的独立危机。`>20%` 的 22 条尤其需要报告覆盖了几个独立事件。

应让整个运行时估值链回放同一份留出数据：给定 t0 合约与行情、到 t1 的实际标的变动，预测 t1 IV、期权价格及组合 P&L，再与 t1 真实链比较。先报告误差与失败覆盖，再判断需不需要更多因子。

### 4.5 其它会限制精度的因素

- **路径 σ 的对象不匹配**：持仓里“最近 ATM 存续期权”的期限和行权价由用户持仓决定，不一定能代表未来 H 天的实现方差。主观 σ、σ 放大与杠杆映射的误差可能超过拟议三个参数带的宽度。应独立取与 H 匹配的市场锚点并验证 RV 条件分布。
- **点差不是保证下限**：B2 只覆盖所选事件、Put 范围和前后都有报价的合约。相对点差的中位数改善，不能证明任意持仓、时点和数量都可保守成交。应分开理论市值、参考 bid/ask 价值和压力流动性情景。
- **收益基准混合是产品选择，不是相同现金流的总盈亏**：主账本是成本口径 P&L；联动账本是相对今日 mark 的增量。这方便观察“保护多补多少”，但紫线不应无条件标成账户净值变化。建议并列“原成本口径 + 保护增量”和“全部入选资产相对今日快照的 ΔNAV”；只要求后者无冲击时为零。
- **每周权利金**：现代码把 `weekly × N/7` 全额加到合计，未模拟新增空头负债及回购/行权损失。它只能是独立的假设净收入预算，不是卖出新期权必得的利润。预测模式默认不计或明确作为外生输入，未来策略模拟必须同时记资产负债。
- **数值与细节**：121 步 CRR 应对比 241/481 等更密网格并检查临界行权区；固定连续股息率不是离散除息日模型。UI 的 5%/10% 价外阈值与代码 `abs(log(K/S))` 不完全相同（后者 0.10 对应约 9.52% 算术价外），宜统一文案/定义。利率保持当前曲线是情景假设，不是对未来曲线的预测。

## 5. 对校准带方案的逐项评价

### 5.1 可保留的部分

保留中线、按**组合合计**取值而非逐腿拼最坏值、缺成员时明确隐藏带子、纯函数与 UI 分层、缓存、卡片/悬停/纵轴同步，都是正确方向。§8 已承认不是统计置信区间、也不包含全部模型风险，这一点很好；但标题、开篇和上下沿算法还需要与这一限制一致。

“中线不变”应从本轮基础问题修复后的新基线开始。不能为了逐字节兼容而永久冻结错误的结算结果。

### 5.2 P1：目前三个端点并非同一统计对象的不确定区间

| 参数 | 现稿端点的实质 | 问题与建议 |
| --- | --- | --- |
| p `[0.50, 0.76]` | 理论平方根规则 vs 最小二乘点估计 | 模型选择与估计量差异，不是 p 的下/上界；可命名为期限规则敏感性场景，或对同一估计器做按事件重采样 |
| OTM `[0.35, 0.65]` | 10–20% 档均值 vs 20%+ 档中位数 | 群体和统计量同时变了；不能用另一组样本的中位数作本组均值/中位数的上界。甚至无普遍保证 low ≤ high |
| β 倍率 `[0.80, 1.25]` | 历史前后段回归值之比 | 没有明确相对于当前全样本 β 的归一化；§2“各档再取中位”和 §5“跨档最小/最大”矛盾；低样本继承值不能冒充该时期的独立估计 |

测试 11 的预设也有算术问题：β=0.8 与 1.2 的前后比值为 2/3，反向为 1.5；即使以等权全样本 β=1 归一化，也是 0.8/1.2，不能由这个夹具自然推出 0.8/1.25。不能先规定想要的区间，再让脚本“生成”它。

短期可以透明地命名“历史规则敏感性场景”，承认规则选择与取整包含判断。若做估计不确定性，先固定同一统计目标，再按事件或连续时间块联合重采样，保留 β、p、OTM 的依赖；对时序数据使用块重采样而非假装 IID 有成熟方法依据，但七次事件仍不足以保证稳定尾部覆盖。[Künsch 的原始研究](https://www.researchgate.net/publication/2355926_The_Jackknife_And_The_Bootstrap_For_General_Stationary_Observations)讨论了相关序列的块 bootstrap。

### 5.3 P1：8 个角 + 中线不能保证连续参数空间的上下界

方案已正确识别组合 P&L 不单调，却仍只采端点。非单调也意味着极值可能在内部，而不只是出现在另一角。

本轮用真实 `buildStressTestSeries` 复现：200 股成本 $100，4 张 Long K90 Put（剩余 180 天，权利金 −$4,000），4 张 Short K120 Put（360 天，权利金 +$14,000）；现价 $100、场景 $90、初始 IV 20%、r=q=0，美式 121 步；关闭期限/价外折扣，基准 β=`autoBetaForDrop(-10)`≈0.9667。尚无 `betaScale` 字段，因此审计通过现有手填 β 入口传入“自动 β × 倍率”，在该点数值等价，未修改生产函数。

| β 倍率 | 合计 |
| --- | ---: |
| 0.80，角点 | −$2,072.38 |
| 1.00，中线 | −$2,064.61 |
| 1.25，角点 | −$2,082.42 |
| **0.90，区间内部** | **−$2,062.21** |

内部结果高于拟议上沿 $2.39；按数量放大会等比例放大。此时另两维关闭，八角退化为两个 β 端点，正是现有 UI 合法的开关组合。另附同到期欧式平滑反例，仍漏上沿 $0.72，说明不只是 CRR 网格噪声。例子用于反证边界保证，并非声称该小组合的误差已达到重大金额。

可选处理：

- 保留九成员，但明确为“这九个离散场景的包络”，不要宣称覆盖整个参数区间。
- 需要连续参数敏感性范围时，加入内部网格/低差异采样和极值邻域细化；记录分辨率、收敛误差。未证明单调或没有数值界时，仍称近似极值。
- 若用联合 bootstrap 参数样本，画样本分位带并标明含义，不把样本 min/max 当成已证明边界。

现稿测试 4 只验证“最乐观角不是最大值”，不足以捕获本问题。需新增内部极值与加密采样比较；中心本来就参与 min/max，因此“中心被夹住”只是程序构造结果，不是统计校准验证。

### 5.4 P1：IV 不变不是组合统一的保守下限

`bandIncludeFlatIv` 只影响 lower 是错的。组合净 Short Vega 时，IV 不变可能比所有正 IV 冲击成员都好。

审计夹具的 4 张 Short ATM Put：正冲击采样 upper=$8,048.75，IV 不变=$9,146.19。若按现稿只更新 lower，勾选“包含 IV 不变”后，这个场景依然在带外。

应把 flat IV 当普通完整场景，同时参与 lower/upper。更一般地，不要把“多头期权的保守假设”直接命名为“组合的保守假设”。

### 5.5 P2：性能估算可行，但调度与降级方法不正确

本轮在 Node v26.4.0、同执行上下文加载真实脚本，用 **23 个本账本 + 31 个联动不同合约、61 点、9 次串行 sweep** 测得总计约 **386 ms**，单次约 41–53 ms；包含计算，不含浏览器渲染/网络。这支持 0.3–0.6 秒量级有工程可行性，不构成所有设备承诺，也不是已实现 band 的实测。

需要修正：

- 张数不等于计算行数：同一合约 31 张只需定价一次乘数量；复杂度应按不同合约和历史事件量估算。
- `requestAnimationFrame` 在下一次 repaint **之前**调用回调，不能把 CPU 计算移出主线程，甚至可能延迟刚画的中线。应选 Worker 或真正有让出机制的分块任务。见 [MDN requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame) 与 [Web Workers](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers)。
- 超时从 8 角减少到 4 角会缩小/改变被探索的参数集合；“区间已简化”不能消除这个数值含义变化。不要让同一账户风险因电脑快慢而不同。可延期、取消、显式不可用，或使用有误差标记的数值近似。
- Worker/缓存必须带 `bookId + ledger revision + snapshot id + scenarioAsOf + 全部估值参数 + calibrationVersion + generation`；切账本/日期/连接/报价后拒绝旧计算结果。大循环取消要用终止旧 Worker 或分块检查，不能仅期望繁忙 Worker 立即处理新消息。
- Node VM harness 注入外部 `Math` 会产生跨上下文调用成本：同夹具在现有 harness 下约 17.7 秒。不要把这个值当浏览器耗时，也不要用它决定用户端降级；应在真实浏览器/同上下文做基准。

### 5.6 其它规格与验收需要补齐

- “基准点及全部上涨侧零宽”只在**实际联动标的跌幅为零/正、其它驱动也相同**时成立。负倍数时主标的上涨对应指数下跌；负倍数有路径损耗时主标的中心点也可能对应指数下跌。按 `linkedChangePct` 判定，不按主图左右判定。
- 没开联动时，目前 β 冲击入口依赖 `linkedHedge`，不存在独立的本账本 β 配置；§4 的“无联动绿带”需要新增明确入口或收窄适用范围。
- 关闭 tenor/OTM 或使用手填 β 时，哪些维度仍变化必须说明；不能偷偷覆盖用户开关。用户手填 p 在默认区间外时，中心与带的含义也要明示。
- 正初始 IV + 非负 β 冲击 + 正折扣/衰减，不会只在某角变成 IV≤0；测试 6 的夹具按当前参数域造不出来。应测试真实可达的缺数据、错误模型、非有限输出或明确注入估值失败。
- 检查每个成员每一点 `headlinePnl` 都是有限数值、点位对齐、单位一致。`series.available` 本身不足以保证所有待画字段有效；null 绝不能被 min/max 当成零。
- 每个参数样本应完整计算整条曲线；逐点边界拼接不一定对应一条可实现的账户路径，更不能从中直接推导爆仓概率。
- 版本化导出校准制品：数据范围、源/缓存版本、过滤规则、独立事件数、估计器、参数样本/端点、运行时版本。单测合成聚合算法不会自动保证手抄的前端常量等于真实报告。

## 6. 推荐落地顺序与可验证标准

### 第一步：修正估值基础，不增加预测复杂度

处理 F1–F5；明确成本口径与 ΔNAV；冻结快照并建立今日重定价门槛；统一到期时刻；给单日杠杆映射正确边界。保留账本事实来源、Long/Short 成本分桶和禁止自动记账的约束。

验收采用独立现金/持仓 oracle，而不只是复算生产公式：买入→持有→平仓/到期/行权、同日截止前后、纯期权、交割后零/负股数、零冲击 ΔNAV=0、报价/币种缺失停算。F1 修复后的行为建立新的中线基线。

### 第二步：交付诚实的参数敏感性带

先选择“九个明确离散场景包络”或“带内部搜索的近似参数范围”，更名并说明不包含路径/流动性/模型全部风险。flat IV 作为普通成员；保留参数开关；Worker 做完整范围的可取消计算。可先不宣称概率与校准覆盖率。

验收增加内部极值、净 Short Vega、反向杠杆、空仓/缺项、跨请求竞态；在不同合约数、长流水和目标浏览器上测 P50/P95、主线程阻塞及取消响应，而非只打印一次耗时。

### 第三步：先验证完整历史预测，再升级为统计区间

QQQ 链已有基础数据，可做滚动留出或留一危机验证。训练与测试按时间/事件分开；重叠 H 日窗口在分界处留出相应间隔，避免训练终点进入测试区。校准样本量报告独立事件数，而非仅合约行数。

至少比较：固定 IV、当前经验模型、含持有期/初始波动率状态的候选模型。输出按 H、DTE、价外程度、涨跌和危机分组的 IV 误差、价格绝对/相对误差、组合 P&L 误差、无法估值比例。研究行情与 TWS 约定不同，先做基准价重定价一致性，避免把数据商定价偏差误认为模型预测偏差。

用同一次事件/时间块重采样产生联合参数成员；若声称例如 80% 的**结果预测区间**，还须加入条件残差/路径不确定性，并在留出集实测覆盖率及带宽。参数估计区间不能替代未来结果区间；少数危机不支持精确尾概率时应明确说样本不足。

### 第四步：需要真实未来路径/风险时再增加路径模拟

先实现少量确定性路径、到期日现金结算、股票交付与 carry，再考虑历史路径重采样/蒙特卡洛。TQQQ 关系必须用相匹配的历史数据检验。若要回答“扛不扛得住”，另建保证金/现金流/最大途中亏损视图；终点盈利并不保证途中不会被迫平仓。

这一路径可以逐步落地，无需一开始重写账本或引入全套 Heston/SVI。增加模型复杂度的依据应是留出误差改善，而不是图更平滑或控件更多。

## 7. 本轮验证结果与边界

已运行：

```text
node tests/run.js
  1012 passed, 0 failed

<项目配置的 Python> -m unittest \
  tests.stress_model_validation_test tests.cost_basis_store_test \
  tests.cost_basis_ws_test tests.cost_basis_executions_test tests.ib_server_ws_test
  Ran 302 tests — OK

node scripts/audit_cost_basis_stress.js
  2 controls passed, 8 invariants violated; exit code 1
```

新增审计脚本完全离线，使用真实页面导出的函数，未替换生产函数。8 个违反项 = 4 个实现边界（F1–F4）+ 1 个单日模型边界 + 3 个方案反例（美式内部极值、欧式内部极值、flat IV 上界）。**这不是“新增八个已修复测试”**；退出码 1 是当前问题仍存在的验收信号，未纳入现有常规 runner。F5 为静态路径检查，未计入这八项；今日收敛偏差另作诊断输出。

系统默认 Python 最初缺少 `ib_async`；改用 `config.local.ini` 已配置的解释器后，上述相关套件全部通过，没有安装/更改依赖。

未做：真实 TWS 盘中逐 tick/跨市场验证、真实账本写入、浏览器视觉/长时间交互验收、完整历史链重新采集与联合校准。本次研究数字引用仓库的 `STRESS_MODEL_VALIDATION_2026-09-05.md`，不冒充新一轮真实数据复算；校准带尚未实现，反例验证的是方案声称的数学性质。

最终建议：**先修基础估值和日杠杆假设；将现稿改为可解释、可取消计算的敏感性场景带；有了整条预测链的留出验证后，再讨论“校准区间”和概率。**
