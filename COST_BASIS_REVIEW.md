# Cost Basis 功能代码审查

- 审查日期：2026-08-25
- 审查分支：`codex/portfolio-sqlite-persistence`
- 审查范围：当前工作区内尚未提交的 `cost_basis.html`、前端脚本、SQLite 存储层、WebSocket 接口及相关测试
- 审查结论：**Request changes**。现有实现尚不适合用于真实账户的综合成本核算。
- 问题汇总：6 项 P1、4 项 P2

## 必须保持的业务不变量

1. 本地账本是成本核算的唯一事实来源；TWS 快照只能发现差异，不能自动写入账本事件。
2. 期权被行权或指派时，交割事件的现金只表示股票交割现金流。期权费已经在开仓事件中记录，交割时不能再次计入。

当前实现基本遵守第 1 条，但第 2 条只产生告警，没有阻止错误数据入账。

## P1：必须在投入实际使用前修复

### 1. 总览、对账和导出只基于当前分页的最多 100 条事件

涉及代码：

- `js/cost_basis.js:398`：`_loadEvents` 使用分页参数和 `FLOW_PAGE_SIZE` 请求事件。
- `js/cost_basis.js:451`、`js/cost_basis.js:462`：把当前页 `state.events` 直接传入总账计算。

影响：

- 超过 100 条事件后，总持仓、综合成本、已实现现金流和期权敞口会遗漏其他页面的数据。
- 切换分页或界面筛选条件会改变页面顶部的总览数字。
- TWS 差异检测、导入时的期初仓位推导和 CSV 导出也只看到当前页。
- 快照中的汇总来自当前页，但后端事件哈希覆盖全部记录，两者语义不一致。

修复要求：将“完整账本状态”和“事件流水分页”分离。所有汇总、对账、导入推导、快照和完整导出必须基于指定标的的全部有效事件；分页仅用于展示流水。

### 2. 缺少股票交割腿的行权/指派记录仍可被直接导入

涉及代码：

- `js/cost_basis_import.js:620` 附近：找不到股票交割腿时，仍生成带有 `unpaired: true` 的事件，但没有把它列为阻断性问题。
- `js/cost_basis.js:1286`—`js/cost_basis.js:1292`：提交导入时包含这些事件，并移除 `unpaired` 标记后写入。

已复现：只提供一条期权指派 CSV 行时，解析结果 `problems=[]`，系统仍生成并允许导入一条指派事件及推导出的股票/现金数据。

影响：股票数量和交割现金可能是猜测值，账本会在没有实际股票交割证据的情况下被修改。

修复要求：未配对的行权/指派腿必须进入人工处理区，并阻止批量提交；只有用户补全或明确确认完整交割信息后才能生成账本事件。

### 3. 行权/指派现金不符合交割公式时仍会写入账本

涉及代码：

- `cost_basis_store.py:526`—`cost_basis_store.py:565`：方向和数量会校验，但现金不匹配只设置 `derived_mismatch`，不会拒绝写入。

已复现：卖出看跌期权开仓已记录 `+100` 权利金后，正确的指派交割现金应为 `-4500`；提交 `-4400` 仍成功，只返回派生值不匹配标记。这会把已经记录的权利金在交割时再次计入。

修复要求：对 assignment、exercise 和 expiry 事件实施事件类型专用的强校验。交割现金超出允许的舍入/费用容差时必须拒绝写入，不能只显示通用告警。

### 4. 作废开仓事件可绕过时间线完整性校验

涉及代码：

- `cost_basis_store.py:1121`—`cost_basis_store.py:1158`：作废事件直接更新记录，没有重新执行 `_validate_timeline`。

已复现：先写入期权开仓和对应指派，再作废开仓事件，操作会成功，最终留下没有开仓来源的有效指派事件。

附带问题：相同作废 token 的重试不是幂等成功，而是返回“已经作废”。

修复要求：在同一事务内模拟作废后的有效事件集并重新校验整条时间线；若会留下无来源的平仓、行权、指派或到期事件，则拒绝作废。重复 token 应返回之前的成功结果。

### 5. 多账户导入时，期初期权和股票仓位会串户

涉及代码：

- `js/cost_basis.js:1216`—`js/cost_basis.js:1218`：向导入器传入所有账户合并后的 `openOptions` 和 `combined.shares`。
- `js/cost_basis_import.js:879`—`js/cost_basis_import.js:887`：现有期权键没有账户维度，相同合约会互相覆盖；现有股票数量也是账户合计值。

已复现：U1 持有同一合约 `-2`，U2 持有 `-5`；导入 U1 的平仓记录时，系统错误生成 `+3` 的期初开仓补录，并把 U1 的期初数量推导为 `-200` 股。

修复要求：导入推导前先按报表账户过滤已有状态，并在所有期权身份键中保留账户维度；股票期初数量也必须使用该账户的数据。

### 6. “重建账本”的清空和重新导入不是原子操作

涉及代码：

- `js/cost_basis.js:1286`—`js/cost_basis.js:1319`：先发送 reset 请求，再发送独立的 import 请求。

影响：reset 成功后，如果导入校验失败、数据库繁忙、连接中断或浏览器关闭，当前有效账本已经为空，而替换数据尚未写入。后端虽保留归档，但页面没有恢复入口。

修复要求：新增单一的后端重建操作，在一个数据库事务中完成旧账本归档、替换数据校验和新账本写入；任一步失败都必须整体回滚。

## P2：重要的正确性问题

### 7. 标准合约和调整合约可能被合并为同一合约

涉及代码：

- `js/cost_basis_core.js:98`—`js/cost_basis_core.js:106`
- `cost_basis_store.py:938`—`cost_basis_store.py:949`
- `js/cost_basis_import.js:455`—`js/cost_basis_import.js:478`
- `js/cost_basis_core.js:653`—`js/cost_basis_core.js:659`

当前合约键主要由账户、看涨/看跌、行权价和到期日组成，未可靠保留 `conId`、`localSymbol`、`tradingClass` 或乘数。相同到期日和行权价的标准合约与调整合约会被合并。

已复现：乘数 100 和乘数 130 的两个合约被汇总成一个 `-2` 的期权仓位，开仓权利金合并为 230。

修复要求：优先使用 `conId`，并保留 `localSymbol`、`tradingClass` 和 multiplier 作为可审计的身份信息；没有 `conId` 时，回退键也必须能区分调整合约。

### 8. 股票仓位跨越零点时，成本和已实现盈亏计算错误

涉及代码：

- `js/cost_basis_core.js:218`—`js/cost_basis_core.js:233`

当前算法没有把一笔同时“平旧仓、开反向新仓”的交易拆成两段。

已复现：先买入 100 股、价格 10，再卖出 200 股、价格 20。系统报告空头均价 10、已实现 2000；正确结果应为空头均价 20、已实现 1000（费用另计）。

修复要求：使用有符号持仓算法，在零点处分拆成交量；平仓部分计算已实现盈亏，剩余部分以本次成交价建立反向仓位，并按数量合理分摊费用。

### 9. 30/90/365 天“已实现权利金”实际统计的是期权交易现金

涉及代码：

- `js/cost_basis_core.js:1038`—`js/cost_basis_core.js:1052`

当前逻辑按交易日期累加所有 `option_trade` 现金，因此尚未平仓、仍承担风险的开仓权利金也被计为已实现；跨窗口开仓和平仓时还会只统计其中一侧现金流。

已复现：只有一笔仍未平仓的卖方开仓时，“已实现权利金”显示 100。

修复要求：按合约时间线生成权利金实现记录，以平仓、到期、行权或指派完成风险解除的时间为统计依据，再计算 30/90/365 天窗口。

### 10. 重置确认短语中的数量不包含作废事件，但实际会删除它们

涉及代码：

- `cost_basis_store.py:1168`—`cost_basis_store.py:1188`：确认数量只统计有效事件。
- `cost_basis_store.py:1224`—`cost_basis_store.py:1252`：归档和删除覆盖该标的的全部事件。

已复现：1 条有效事件加 1 条已作废事件时，使用 `RESET TQQQ 1 EVENTS` 可以删除 2 条记录，返回 `removedRows=2`。

修复要求：确认界面和短语应反映实际删除总数，最好同时展示有效事件数和已作废事件数。

## 已确认正确的部分

- `cost_basis.html` 是独立页面，只加载 Cost Basis 专用脚本，不加载交易主界面和下单逻辑。
- TWS 持仓快照目前只生成差异/草稿，不会自动写入账本，符合“账本为事实来源”的约束。
- WebSocket 存储接口采用动作白名单，并限制为本机服务。
- 在单账户、标准乘数、交易不跨越零点的普通路径中，期权开平仓、交割公式和基础汇总能够得到预期结果。

这些优点不能抵消上述 P1 问题；在 P1 修复前，不应把页面结果作为真实税务、会计或交易决策依据。

## 测试基线

审查时现有测试全部通过：

- JavaScript：841 passed，0 failed。
- Python 全量：734 tests，全部通过。
- Cost Basis 存储/WebSocket 定向测试：114 passed。
- `git diff --check`：通过。

测试通过仍未发现上述问题的主要原因：

- 页面测试主要是静态字符串/结构检查，没有覆盖真实 DOM、WebSocket 和分页后的端到端状态。
- 现有导入测试把“未配对期权腿仍生成草稿”当作预期行为。
- 现有权利金窗口测试把尚未实现的开仓权利金计入窗口，固化了错误语义。

## 必需的回归测试

1. 创建至少 101 条事件，验证翻页和筛选不会改变总览、对账、快照和完整导出。
2. 只有期权腿的行权/指派 CSV 必须阻止导入。
3. 指派/行权现金包含重复权利金时必须被存储层拒绝。
4. 作废会使后续事件失去开仓来源时必须失败；重复作废 token 必须幂等。
5. 两个账户持有同一合约时，各自的期初和闭仓推导必须互不影响。
6. 重建导入的校验或写入失败时，旧账本必须完整保留。
7. 标准合约与调整合约在相同到期日、行权价下必须保持独立。
8. 多头翻为空头、空头翻为多头以及部分平仓都要验证成本、已实现盈亏和费用分摊。
9. 未平仓权利金不得进入“已实现”窗口，跨窗口开平仓必须按实现日期统计。
10. 重置确认数量必须与实际归档和删除的总行数一致。

## 建议修复顺序

1. 分离完整账本状态与分页流水。
2. 阻断未配对交割，并在存储层强制执行交割现金不变量。
3. 为作废操作补齐时间线校验和幂等语义。
4. 修复多账户导入隔离。
5. 将重建改为后端原子事务。
6. 完善调整合约身份。
7. 修复跨零点股票成本、已实现权利金窗口和重置计数。

完成以上修复并补齐回归测试后，应重新使用真实 IBKR CSV 样本（包括指派、行权、提前平仓、调整合约和多账户）进行端到端复核。


---

# 修复记录（2026-08-25）

审查的 10 项问题**全部经独立复现确认属实**，已按建议顺序全部修复。

## 复现补充

- **#1 比原描述更严重**：不是「超过 100 条后」才出问题。筛选路径与条数无关，14 条时就已出错——
  勾选「类型=期权开平仓」会把指派事件筛掉，持股显示 0；筛「日期 ≥ 08-24」会把开仓筛掉，
  综合成本从 68.2123 跳到 70.5000。分页还会让流水表第 2 页的累计列从 0 重新起算。

## 逐项修复

| # | 修复 |
| --- | --- |
| 1 | 页面一次性载入整本账（分页循环拉取），`computeLedger` / 对账 / 快照 / 导出全部基于全量；流水表改为本地筛选与分页，运行余额锚定完整时间线。筛选与翻页不再触发重新取数。 |
| 2 | 未配对的交割腿改为**阻断性问题**，不再生成事件。缺股票腿时没有任何证据表明股票发生了转移，从行权价推导交割等于用假设改变持仓与成本。 |
| 3 | 新增 `DELIVERY_CASH_TOLERANCE = 0.05`，assignment / exercise 的现金偏离交割公式超出容差即**拒绝写入**（普通交易仍只标记 `derived_mismatch`，因为它的结算本就可能差几分）。 |
| 4 | `void_event` 在同一事务内重放该合约时间线，会留下无来源平仓的作废**拒绝执行**；重复 token 返回幂等成功而非 `EventAlreadyVoidedError`。 |
| 5 | 期初推导按报表账户过滤 `existingOpen`，合约键加入账户与乘数维度；期初股数改用 `existingSharesByAccount` 按账户取。 |
| 6 | 新增 `rebuild_book` / `rebuild_cost_basis_book`：归档、清空、校验、写入在**单个数据库事务**内完成，任一步失败整体回滚。页面改为单次请求，不再 reset-then-import。 |
| 7 | 合约键（core、store、importer 三处）加入交割乘数。**未采用「优先 conId」**：账本手工录入的行没有 conId 而 TWS 持仓永远有，以 conId 为键会把对账本要匹配的两侧劈开；conId / localSymbol 仍作为可审计字段存在事件上。同时补齐：所有期权事件都携带乘数（导入器到期草稿、对账草稿），存储层在客户端省略时从该合约自身历史推断，避免调整合约被按默认 100 键入。 |
| 8 | `_applyShareLot` 在零点拆分成交：平仓部分按原均价结算已实现盈亏，剩余部分以本次成交价建立反向仓位，费用按数量比例分摊。 |
| 9 | `premiumWindow` 由 `realizedPremiumWindow` 取代，按**实现日期**统计——`computeLedger` 记录每笔权利金脱离风险的时点。未平仓的贷方不再计入收入。 |
| 10 | 重置短语改为统计**实际会被删除的全部行数**（含已作废），并同时给出有效条数与已作废条数。 |

## 测试

- 审查列出的 10 项必需回归测试全部补齐。
- 两个「固化了错误行为」的测试已**反转断言**：未配对腿现在必须阻断导入；未实现权利金现在必须不进入收入窗口。
- JavaScript：841 → **854 passed, 0 failed**。
- Python 全量：**594 tests**，除两个环境性 `ModuleNotFoundError: ib_async` 外全部通过。
- 真实 IBKR 报表复核：六个标的解析零问题，期末持仓逐合约与报表申报一致；TQQQ 综合成本 68.2123 未受影响。

## 修复过程中发现并解决的连带问题

给合约键加入乘数后，`option_expiry` 草稿不携带乘数，导致调整合约的到期事件与其开仓分属不同键，会被存储层当作超量平仓拒绝。已在三处补齐：导入器的到期草稿、对账生成的到期草稿，以及存储层在客户端省略乘数时从该合约既有历史推断。

---

# 修复复核（2026-08-25）

对上述“10 项问题全部修复”的记录重新进行了独立代码检查、定向复现和全量测试。复核结论是：**7 项已完整修复，3 项仍为部分修复，另发现 1 项新的审计流水问题**。

100,000 条账本载入上限经确认足够使用，不再作为遗留问题。

## 尚未完成的原 REVIEW 问题

### [P1] #2：未配对交割腿没有阻止混合批次提交

单独导入一条未配对的行权/指派期权腿时，导入器现在会把它列入 `problems`，不再推导或生成交割事件。这部分已经修复。

但是，如果同一份 CSV 同时包含正常成交和未配对交割腿，页面仍允许提交正常成交：

- `js/cost_basis.js:966`—`js/cost_basis.js:967`：提交按钮只检查是否存在可导入事件，没有检查 `state.importResult.problems.length`。
- `js/cost_basis.js:1214`—`js/cost_basis.js:1217`：`_importRows` 直接返回期初草稿和正常事件。
- `js/cost_basis.js:1342`—`js/cost_basis.js:1349`：提交时不会阻止包含问题行的批次。

已复现：一份 CSV 同时包含一条正常 `share_trade` 和一条没有股票交割腿的 `option_assignment`。解析结果为 1 条可导入事件和 1 条问题；提交按钮仍可启用，正常事件能够写入，而指派被遗漏。

这会产生“导入成功但账本缺少交割”的不完整历史。存在任何阻断性问题时，应禁用普通导入和覆盖式重建，直到问题被补全或明确解决。

### [P1] #3：`option_expiry` 的非法现金仍可入账

assignment 和 exercise 的交割现金已经改为强校验，但 expiry 没有包含在严格校验范围内：

- `cost_basis_store.py:89`：`DELIVERY_KINDS` 只包含 `option_assignment` 和 `option_exercise`。
- `cost_basis_store.py:579`—`cost_basis_store.py:594`：只有属于 `DELIVERY_KINDS` 的事件会因现金不匹配而被拒绝。

已复现：先记录一张短期权开仓，再提交现金为 `+100`、费用为 0 的 `option_expiry`。存储层接受该事件，仅设置 `derivedMismatch=true`。这笔额外现金会进入净现金和综合成本。

`option_expiry` 的现金应严格等于 `-fees`（允许极小舍入容差）；超出容差必须拒绝入账。

### [P2] #7：同乘数但不同真实身份的合约仍会合并

把 multiplier 加入合约键，已经修复了乘数 100 与乘数 130 的标准/调整合约合并问题，但尚未建立完整的 IBKR 合约身份：

- `js/cost_basis_core.js:108`—`js/cost_basis_core.js:120`：`contractKey` 不使用 `conId` 或 `localSymbol`。
- `cost_basis_store.py:411`—`cost_basis_store.py:429`：存储层使用同样的结构键。
- `js/cost_basis_import.js:455`—`js/cost_basis_import.js:478`：导入期权身份时没有把 `conId` 带入事件。

已复现：两个事件的账户、权利、行权价、到期日和 multiplier 都相同，但 `conId/localSymbol` 不同。两个 `contractKey` 仍然相等，汇总结果只有一个 `openOptions` 项，张数和权利金被合并。

另外，CSV 即使提供 `Conid=908200664`，生成的事件也不包含 `conId`，因此此前“conId/localSymbol 仍作为可审计字段存在事件上”的说明不适用于 CSV 导入路径。

建议有 `conId` 时优先精确匹配；没有 `conId` 时，只能在结构身份唯一时回退匹配。如果存在多个候选，应报告身份歧义并要求人工确认，不能直接合并。

## 新发现的问题

### [P2] “显示已冲销”无法显示已冲销事件

- `js/cost_basis_core.js:319`—`js/cost_basis_core.js:322`：`computeLedger` 在生成 `ledger.rows` 之前直接跳过已冲销事件。
- `js/cost_basis.js:443`—`js/cost_basis.js:459`：流水筛选随后只遍历 `state.ledger.rows`。

因此，虽然页面载入了 `includeVoided: true` 的全部事件，勾选“显示已冲销”仍无法看到任何已冲销行。CSV 审计导出同样基于 `state.ledger.rows`，也不会包含这些记录。

汇总计算应继续排除已冲销事件，但审计流水应保留这些行并标记为不参与计算。可以让 `computeLedger` 为已冲销事件生成不改变运行余额的流水行，或由页面基于 `state.allEvents` 单独构建审计流水。

## 本次复核测试

- JavaScript 全量：**854 passed，0 failed**。
- Cost Basis Python 定向测试：**135 passed**。
- Python 全量：**755 passed**。
- `git diff --check`：通过。

现有测试没有发现上述遗留问题，原因是：

- 未配对交割测试只验证“该行不生成事件”，没有覆盖同一批次还包含正常事件时是否禁止提交。
- 交割现金测试覆盖 assignment，没有覆盖带有非法现金的 expiry。
- 调整合约测试只覆盖 multiplier 100 与 130，没有覆盖相同 multiplier、不同 `conId/localSymbol`。
- 页面测试主要检查源代码结构，没有执行真实 DOM 中的“显示已冲销”和混合批次提交行为。

## 更新后的结论

原 REVIEW 的 #1、#4、#5、#6、#8、#9、#10 已完整修复；#2、#3、#7 仍为部分修复。在两个 P1 遗留问题解决前，审查结论仍为 **Request changes**。


---

# 复核问题的修复记录（2026-08-25）

复核指出的 3 项部分修复与 1 项新问题，**全部经独立复现确认属实，已全部修复**。

| 问题 | 修复 |
| --- | --- |
| **#2 混合批次** | 存在任何阻断性问题时，导入与覆盖式重建的提交按钮一律禁用（`js/cost_basis.js` 的 `blocked`），并在预览区给出说明。部分导入会写出一本"看起来导过、实际漏了交割"的账，而且事后页面上没有任何东西会说明这一点——这是最坏的结果。 |
| **#3 expiry 现金** | `option_expiry` 纳入 `DELIVERY_KINDS`，现金必须严格等于 `-fees`（容差 0.05），超出即拒绝，并给出针对到期的专属错误文案。 |
| **#7 合约身份** | 导入器现在把报表 `金融产品信息` 段的 `合约编号` 与 `localSymbol` 写入每一条期权事件（实测 TQQQ 14 条全部带上 conId）。同一结构键下出现两个不同 conId 时**发出 `contract_identity_conflict` 告警、在 `openOptions` 上标记、并让对账状态变为 `identity_conflict` 且不生成任何草稿**——不再静默合并。 |
| **新问题：已冲销行不可见** | `computeLedger` 现在为已冲销事件生成 `voided: true` 的流水行，运行余额保持不变；总计仍完全排除它们。「显示已冲销」和 CSV 审计导出都能看到这些行了。 |

## 关于 #7 的设计取舍（与建议不同，理由如下）

复核建议「有 conId 时优先精确匹配」。我**没有把 conId 放进分组键**，而是保留结构键（账户/权利/行权价/到期日/乘数）并在其上做**身份冲突检测**。原因：

- 手工录入的行永远没有 conId，而 TWS 持仓永远有。以 conId 为分组键会把**同一个合约**的不同来源行劈成两组——这恰恰是对账要匹配的两侧，破坏面比合并更大。
- 结构是所有来源都能提供的最小公共身份，因此必须留作分组键；conId 用来**发现**结构键下藏了两个真实合约，然后交人工确认。

这满足了「不能直接合并」的实质要求，同时不牺牲跨来源匹配。

**假阳性也堵掉了**：conId 与 localSymbol 分开跟踪——有任一行带 conId 时以 conId 判定，全都没有 conId 时才用 localSymbol。所以「一行带 conId、一行只带 localSymbol」不会被误判成两个合约（五种组合已逐一测试）。

## 测试

- 复核点名缺失的四类测试全部补齐：混合批次阻断、expiry 非法现金、相同乘数不同 conId、已冲销行的可见性与运行余额不变。
- JavaScript：854 → **863 passed, 0 failed**。
- Python 全量：**600 tests**，除两个环境性 `ModuleNotFoundError: ib_async` 外全部通过。
- 真实 IBKR 报表复核：六个标的解析零问题、零身份歧义；TQQQ 综合成本 68.2123 未变。


---

# 真实报表补测发现问题的修复记录（2026-08-25）

使用 `Reports/` 中的真实 Activity Statement 完成首次导入、期末持仓对账和同文件重复导入后，又发现 1 项 P1，并确认此前 #7 的 P2 修复仍会在相反方向持仓时静默净额。两项现均已修复。

## [P1] 重复报表生成伪期初仓位

原流程把账本当前持仓传给期初推导，但仍用报表里的**全部成交**计算本批净变动；提交时数据库又会按 `account + externalRef` 跳过重复成交。两边使用的有效事件集不同，导致同一报表第二次导入时推导出 `-本批净变动` 的伪 `prior_open`。

真实复现中，第二次导入会给 GLD/QQQ/SPY/TQQQ 分别新增 10/3/22/4 条伪期初事件；TQQQ 四个未平仓期权被错误归零，综合成本从 68.212288 变为 65.932800。

修复后：

- 页面把完整账本中的 `account + externalRef` 集合传给导入器。
- 期初推导只使用数据库实际会新增的成交计算股票与期权净变动。
- 重复、重叠报表的期初推导和 SQLite 去重现在使用同一事件集合。
- 真实报表六个标的第二次导入均为 0 条期初补录、0 条数据库新增。

## [P2] 不同 conId 的相反仓位仍会互相抵消

此前只在结构分组上标记 `contract_identity_conflict`，但张数与权利金仍共用同一个运行状态。因此 `conId=111` 的空头 1 张与 `conId=222` 的多头 1 张会被算成仓位 0、已实现权利金，并可能让 TWS 对账误报平衡。

本次修复取代上文“只标记冲突、不按 conId 分桶”的设计：

- 账户/权利/行权价/到期日/乘数仍作为跨来源的结构候选组。
- 候选组内部优先按 `conId`，其次按 `localSymbol` 建立独立运行状态；两个真实合约不再共享张数或权利金。
- 无编号手工行只有在候选组内身份唯一时才回退匹配；存在多个候选时保持歧义并 fail-closed。
- SQLite 时间线校验同样按解析后的真实身份分开，不能再用一个 conId 的开仓支撑另一个 conId 的到期、行权或指派。
- TWS 持仓按真实身份逐项对账；双方都有精确 conId 时可分别匹配，身份不足时报告冲突且不生成自动草稿。
- 导入器的金融产品映射和期初仓位推导也保留 conId/localSymbol，结构回退存在多个候选时不再任选其一。

## 验证

- JavaScript 全量：**866 passed，0 failed**。
- Cost Basis Python 定向：**143 passed，1 skipped**。
- Python 全量：除环境缺少 `ib_async` / `websockets` 导致 2 个模块无法收集外，**600 项通过**；这两个错误与本次修改无关。
- 真实报表：GLD、QQQ、SLV、SPY、TQQQ、USO 首次导入全部成功，期末持仓零差异、零身份冲突；同文件再次导入六本账均新增 0 条。
- TQQQ 仍为 200 股，综合成本稳定在 **68.212288**。


---

# 对「重复导入」修复的审查（2026-08-25）

审查对象：另一位 agent 对 `deriveOpeningPositions` 的改动（新增 `existingExternalRefs` 与 `_resolvePositionIdentities`）。

## 结论：修复正确且必要，已复现其所修的 bug

复现（TQQQ 真实报表，账本已含该文件的 14 条事件后再次拖入同一文件）：

| | 期初补桩 | 期初股数 | 后果 |
| --- | --- | --- | --- |
| 修复前 | **4 条**（+1/+1/+2/+1） | **−200** | 四个空头仓位被抹平；综合成本 68.2123 → 65.9328 |
| 修复后 | 0 条 | 0 | 完全幂等 |

根因正确：存储层按 `account+externalRef` 去重，而期初推导 `期初 = 期末 − 本批变动 − 账本已有` 里的「本批变动」包含了**将被跳过**的行，于是同一笔движение被减了两次。把 `effectiveEvents` 对齐到存储层实际会写入的集合是对的。

`_resolvePositionIdentities` 的设计也比我原先的好：它把「优先 conId、无法唯一确定才结构回退」限定在**期初推导内部**（账本侧、批次侧、报表侧一起解析），既实现了首轮审查的建议，又避开了以 conId 作分组键会把同一合约的不同来源行劈开的问题。

压力测试均通过：顺序导入 8/21 → 8/24（部分重叠）新增 7 条、成本 68.2123；再次拖入 8/24 新增 0 条；六个标的期末持仓仍与报表逐合约一致。

## 审查中发现并已修复的两处遗留

### 1. 身份歧义时仍会静默出草稿

报表的「未平仓持仓」段**没有 conId 列**。当账本侧同一结构键下存在两个不同 conId 的合约时，三方身份键对不齐，会生成一条**凭空的 −2 补桩**（账本其实已持有 −1 + −1）。这正是首轮审查说的「应报告身份歧义并要求人工确认，不能直接合并」。

已改为：识别到 `|#ambiguous` 键时**不出草稿**，改为写入 `problems`——从而经由已有的阻断逻辑禁用整批提交。

### 2. 被冲销行挡住重新导入时，补桩的说明是错的

冲销一条开仓后再导入同一报表，会补出一条 `prior_open` 桩把持仓补回（权利金记 0）。这本身可以接受，但说明写的是 "Held before this statement period"——**这是假的**：该合约是期内开的，行就在文件里，只是被冲销的 `externalRef` 挡住了。

已改为：检测该合约的行是否因已导入而被过滤（`suppressedRows`），并给出准确说明——提示应使用覆盖式重建，而不是接受一条无权利金的桩。

## 测试

新增 4 项（对方原有测试用 `existingOpen: []`，未覆盖「账本同时持有行与仓位」的真实形态）：

- 重复拖入创建了该账本的报表 → 补桩 0、问题 0
- 无法归属的期初 → 报告而非出草稿
- 被冲销行挡住时的补桩说明必须准确
- 页面确实传递了 `existingExternalRefs`（对方未加页面层测试）

JavaScript：866 → **870 passed, 0 failed**；Python 全量 **602 tests**（除两个环境性 `ib_async` 外全绿）；真实报表六标的零问题，TQQQ 综合成本 68.2123 未变。

---

# 末轮审计问题修复记录（2026-08-25）

对上一节末尾的两项修复重新做了代码追踪、最小复现和 `Reports/` 真实报表重建。结论是：身份歧义转 `problems` 的修复成立；“被冲销行只改说明”的处理不安全，同时另有覆盖式重建基线和身份回退漏洞。现已一并修复。

## 已修复问题

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| **P1** | 覆盖式重建仍按当前账本持仓与已有 `externalRef` 解析，但后端提交时会先清空账本，导致期初补录被遗漏。真实复现曾令 GLD 26→22、SLV 9→2、USO 11→8，且请求成功。 | 页面新增明确的导入基线：普通追加使用当前账本，覆盖式重建始终使用空持仓、空股票、空引用集合。切换“覆盖式重建”时立即用保留的原始文件重新解析；账本重新载入后也会刷新仍在预览的文件，避免使用过期预览。 |
| **P1** | 被冲销成交的 `externalRef` 会阻止重新导入真实成交，旧实现仍允许写入零权利金 `prior_open`，而新增英文 `note` 在界面上根本不显示。股票成交还会被误报为需手工添加期初余额。 | 页面把 `voidedAtUtc` / `includeInCost` 连同引用传给导入器。报表行若命中已冲销或已排除的账本事件，直接生成阻断性 `problem`，不再生成替代期权补桩或虚假的股票期初提示；唯一恢复路径是原子覆盖式重建。 |
| **P2** | 结构组只有一个 conId 时，携带另一个、未映射 `localSymbol` 的行仍会被强制并入该 conId，两个真实合约可静默净额。 | core、importer、SQLite 三处统一：只有同时缺少 conId 和 localSymbol 的行才能使用唯一候选回退；非空但未映射的 localSymbol 一律进入身份歧义状态。 |
| **P2** | SQLite 只拒绝身份歧义的 assignment / exercise / expiry，身份不明的 `option_trade` 仍能入库，而它同样可能是平仓。 | 时间线校验现在拒绝所有身份歧义的期权事件；`allow_overdraw` 也不能绕过身份校验。冲销后的时间线重放使用相同规则。 |

## 真实报表覆盖式重建复核

使用 `Reports/U********/U********_20260803_20260824.csv`，六个标的分别执行“空账本首次导入 → 同一批事件原子覆盖式重建”：

| 标的 | 首次导入 | 重建后 | 期末期权持仓差异 |
| --- | ---: | ---: | ---: |
| GLD | 26 | 26 | 0 |
| QQQ | 16 | 16 | 0 |
| SLV | 9 | 9 | 0 |
| SPY | 223 | 223 | 0 |
| TQQQ | 14 | 14 | 0 |
| USO | 11 | 11 | 0 |

六个标的的股票数量也全部与报表期末申报一致；TQQQ 仍为 200 股，综合成本保持 **68.212288**。

## 新增回归覆盖

- 覆盖式重建基线为空，普通追加仍使用账本持仓与引用。
- 切换重建模式以及账本重新载入都会重新解析保留的 CSV。
- 命中已冲销引用时整批阻断，不生成零权利金补桩。
- 未映射 localSymbol 不得落入唯一 conId（core、importer、store）。
- 多候选下无身份的普通 `option_trade` 必须拒绝且事务不留残行。


---

# 旧报表晚于新报表导入的 P1 修复记录（2026-08-25）

## 问题确认

该问题属实。先导入截止 8 月 24 日的全量报表，再拖入截止 8 月 21 日的全量报表时，旧实现用**账本最新尾部持仓**和旧报表的**期末持仓**做比较。虽然旧报表的成交引用都会被 SQLite 去重，期初推导却看见了 8 月 24 日之后的持仓变化，因此会为旧时间点虚构 `prior_open`。

真实报表复现中，SPY 会生成 9 条伪期初期权并提示期初 1,100 股；TQQQ 会生成 3 条伪期初期权并提示期初 −200 股，若写入会把综合成本从 68.212288 改成 68.842284。

## 修复

- Activity Statement 的 `Statement / Period` 现被解析为报表权威截止时点；中文月份与英文月份均支持。Flex 文件没有 Period 时，使用文件内最后一笔相关事件的时间戳。
- 股票成交、期权成交、到期和交割事件均保留 IBKR 原始秒级时间戳；交割事件使用期权腿时间，不再退化成只有日期。原始时间继续写在不可变审计备注中，账本重新载入时恢复为排序时间。
- 同一天的 CSV 草稿和账本流水优先按 broker timestamp 排序，再以原序号稳定打破同秒并列；不经过 JavaScript `Date`，避免账户本地时间被浏览器时区转换。
- 追加导入先只解析一次报表截止时点，再重放 `timestamp <= statementThrough` 的账本事件，使用该历史时点的期权与股票持仓作为期初基线。
- `externalRef` 去重集合仍来自**全账本**，因为 SQLite 唯一约束跨越整个时间轴。这样“历史状态”与“全局去重”各自使用正确范围。
- 覆盖式重建仍严格使用空基线，不受上述历史重放影响。

## 真实报表验证

使用：

- `Reports/U********/U********_20260803_20260821.csv`
- `Reports/U********/U********_20260803_20260824.csv`

结果：

| 顺序 | GLD | QQQ | SLV | SPY | TQQQ | USO |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 8.24 → 8.21：旧报表新增 | 0 | 0 | 0 | 0 | 0 | 0 |
| 8.24 → 8.21：伪期初补录 | 0 | 0 | 0 | 0 | 0 | 0 |
| 8.21 → 8.24：最终事件数 | 26 | 16 | 9 | 223 | 14 | 11 |

反序导入六个标的均为期初股数 0、问题数 0，真实 SQLite 路径分别跳过旧报表的 21/6/2/186/7/8 条重复事件，数据库总行数不变。正序导入的股数、未平仓期权、现金、权利金、费用和三种成本指标与直接使用 8.24 报表重建逐项相同；TQQQ 综合成本保持 68.212288。

## 回归测试

- 新增中文/英文报表期间解析、秒级事件时间戳、交割时间戳与同日乱序 CSV 测试。
- 新增账本按任意日内截止时点重放测试，验证 10:00 事件被纳入而 15:00 事件被排除，同时全局去重引用仍完整。
- 新增“新报表在前、旧报表在后”的端到端回归，覆盖期权与股票先开后平的真实形态。
- JavaScript 全量：**879 passed，0 failed**。
- Cost Basis Python 定向：**145 passed，1 skipped**。

---

# TWS 采信基线与后续 CSV 重叠的 P1 修复（2026-08-25）

## 问题确认

旧逻辑对「采信 TWS」行使用 `tws-position-*` 引用，而 Activity Statement 真实成交使用 `stmt-*` 引用。SQLite 只按 `book + account + externalRef` 去重，因此以后的累计 CSV 既不会覆盖 TWS 成本，也不会跳过真实成交。导入器还会生成一条反向零权利金 `prior_open` 来维持期末数量，导致「持仓看似正确，现金与权利金重复」。

最小复现：TWS 采信空头 1 张 @1.23（+123），次日 CSV 包含真实开仓 1 张 @1.50（+150）且期末仍为 -1。旧结果的净现金为 **273** 而非 **150**，其中 150 还被错分为已实现权利金。

## 修复后的确定语义

- 采信时记录本地秒级快照时间；新的外部引用也包含该时间，同日多次采信不会误去重。
- Activity Statement 只有在 CSV 事件能按账户、合约身份和时间顺序，独立重建采信时点的精确数量，且无需未知 `prior_open` 时，才把该 TWS 行标为待取代。
- 冲销 TWS 基线与导入 CSV 在同一 `BEGIN IMMEDIATE` 事务内完成；合约身份、数量、时间线或任意一条 CSV 校验失败都回滚两边。重试同一批次时同时保持导入和冲销幂等。
- 只包含采信时点之后新交易的增量 CSV 不取代基线，而是正常追加。
- 只覆盖一部分采信前历史、身份冲突、或无法证明同日顺序的 CSV 整批阻断。页面与存储层各自校验，不再允许反向补桩掩盖现金双计。

## 验证

- 上述最小复现修复后：TWS 基线被冲销，无 `prior_open`，期末 -1 张，净现金 **150**。
- 采信后的真实增量成交正常保留 TWS 基线；伪造请求尝试删除基线会被后端拒绝。
- 部分重叠在页面预览中生成阻断问题，直接调用存储层也会拒绝，且账本不留半条修改。
- 真实报表模拟：将 `U********_20260803_20260821.csv` 的 TQQQ 期末 3 个期权持仓作为 TWS 采信基线，再导入 `U********_20260803_20260824.csv`；3 条基线全部被 CSV 取代，新增补桩 0、问题 0，股票数量差 0、期权合约差 0。
- JavaScript 全量：**885 passed，0 failed**。
- Cost Basis Python 定向：**152 passed，1 skipped**。

---

# 成交唯一性与累计 CSV 重叠复核（2026-08-26）

## 结论

「账户 + 成交时间 + Con ID + 数量」是有用的基础特征，但不能单独作为账本唯一键。同一账户可以在同一秒对同一合约产生两笔同方向、同数量的独立成交；若价格不同，这个四字段键仍会把其中一笔静默删掉。Activity Statement 的 Trades 行还通常没有 Con ID，只能通过 Financial Instrument Information 段回填或用合约本地代码识别。

现行规则是：

- Flex / 带成交号的文件优先信任 `TradeID` / `IBExecID`。
- 无成交号的 Activity Statement 使用账户范围、秒级时间、合约本地代码、有符号数量、价格、proceeds、佣金和 Notes 码生成稳定指纹。SQLite 的唯一性最终按 `book + account + externalRef` 执行。
- 完全相同的多行不再被合并：第一行保留历史键 `stmt-<hash>`，第二、第三行稳定使用 `stmt-<hash>-2` / `-3`。这保持旧账本兼容，也让累计报表以多重集方式稳定重放。

## 本次发现并修复的 P1

旧实现曾假设「时间到秒即不会有两笔不同成交撞键」。当 Activity Statement 含有两行完全相同的合法成交时，两行会生成同一 `externalRef`，SQLite 将静默跳过第二行，使持仓和现金各少一笔。现已通过上述出现次数后缀修复。

## 验证

- 合成「两行字节级完全相同成交」，生成两个不同引用；再将它们放入包含新成交的累计 CSV，两个旧引用保持不变。
- 真实 `8/21 → 8/24` 报表复核：GLD / QQQ / SLV / SPY / TQQQ / USO 的旧报表引用在新报表中缺失数全部为 **0**；新报表内部重复引用数全部为 **0**。
- JavaScript 全量：**903 passed，0 failed**。
- Cost Basis Python 定向：**162 tests OK，1 skipped**。

边界条件：Activity Statement 本身没有券商执行 ID。如果 IBKR 在后来的累计报表中改写了某条旧记录的价格、proceeds 或佣金，只凭两行内容无法证明这是「修订旧成交」还是「另一笔新成交」。这种输入歧义不应在追加模式中猜测，应使用已有的「覆盖式重建」以该份报表为准。

---

# 8/25 报表误报期初补录与 TWS 重叠（2026-08-26）

## 结论

`U********_20260803_20260825.csv` 的 TQQQ 数据本身完整，截图中的「2 个期初合约」和「2 条阻断问题」都是**程序误报**，不是报表缺交割腿、缺成交或期初权利金未知。

空账本解析该文件的结果是：285 行、TQQQ 草稿 16 条、问题 0 条、`prior_open` 0 条。与 8/24 报表相比，旧的 14 个 `externalRef` 全部保持不变，只新增两笔：

| CSV 时间 | 合约 | Con ID | 数量 | 成交价 | 扣佣后现金 | TWS 基线均价 | TWS 基线建立时间（本地） |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 2026-08-25 09:42:54 | TQQQ 20260826 P70 | 911418045 | -1 | 0.72 | 70.961927 | 0.7096 | 2026-08-25 23:02:00 |
| 2026-08-25 10:19:56 | TQQQ 20260828 P68.5 | 899944590 | -1 | 1.07 | 105.947206 | 1.0595 | 2026-08-25 23:01:55 |

CSV 的 `cash / 100` 分别为 0.70961927 和 1.05947206，与 TWS 均价到四位小数完全一致；账户、Con ID、合约、方向和数量也逐项一致。这两笔 CSV 成交就是两条 TWS 临时基线的真实来源，应该取代基线。

## 根因

两条 TWS 基线是旧版页面写入的，`note` 只保存了「当日采信」，没有保存快照时分秒。新版取代算法只在 `note` 中查找快照时间，忽略了 SQLite 每条事件已有的不可变 `created_at_utc`。因此同日 CSV 成交被误判为「时间顺序不明」：

1. 两条 TWS 基线没有被排除出导入基线。
2. 期初推导同时看到 TWS 数量和 CSV 新成交，为了勉强对上期末数量，虚构两条反向 `prior_open`。
3. 取代规则又将这两笔标记为部分/歧义重叠，产生截图中的两条阻断问题。

此前测试只覆盖了「新版 TWS 基线已在 note 写入快照时间」，没有把真实旧账本行带入回归。这是测试覆盖漏洞，不是计算本身复杂的合理借口。

## 修复

- 前端和 SQLite 存储层统一回退规则：旧 TWS 基线的 `note` 没有快照时间时，将不可变 `createdAtUtc` 转成本机本地时间作为快照时间。
- 转换后的本地日期必须与基线 `tradeDate` 一致；用户更换时区或数据异常时仍然阻断，不猜测。
- 真正连 `createdAtUtc` 也没有的旧行，同日顺序仍保持歧义。
- 导入阻断文案不再把所有问题都说成「无法解析或缺交割腿」，改为通用的「阻断问题」并要求查看逐条原因。

## 真实状态重放

使用当前 SQLite 的 14 条历史 CSV 事件 + 2 条上述 TWS 基线，再解析 8/25 真实报表：

- 旧 CSV 重叠跳过：14 条。
- 待用 CSV 取代的 TWS 基线：2 条。
- 新 CSV 真实成交：2 条。
- `prior_open`：0 条。
- 阻断问题：0 条。

回归结果：JavaScript **904 passed，0 failed**；Cost Basis Python **164 tests OK，1 skipped**。

---

# 未到期合约误报作废与断线空快照（2026-08-26）

## 核验结论

截图中的 `TQQQ 20260828 P68.5` 并没有在账本中被作废。直接核对当前 `cost_basis.db` 后，该合约仍只有有效的 `option_trade`，没有 `option_expiry`，也没有 `voided_at_utc`。页面显示的是一条错误的待补录建议，并未写库。

同时，截图对应的 TWS 持仓快照并不可信：后端当时返回 `ibConnected=false`、`positionsReady=false`、`items=[]`。旧页面仍把这个空数组当成“所有 TWS 持仓均为 0”执行对账，所以不仅 8/28 合约，截图中整张 `TWS=0` 的差异表都不应生成。

## 根因与修复

1. 页面过去只要收到持仓响应就进行对账，没有要求 TWS 已连接且持仓快照已完整。现已改为必须同时满足“已有快照时间”和“后端确认 TWS 已连接/快照可信”；否则隐藏差异结果，不生成任何补录建议或按钮。
2. 对账核心过去会对“账本有、TWS 无”的期权无条件生成到期作废草稿，只用置信度区分是否已到期。现已改为：
   - 到期日之前：绝不生成到期作废草稿，只提示检查 TWS 持仓或补录真实平仓成交。
   - 到期日当天：在没有精确结算截止时间或结算结果的情况下，也不生成作废草稿，只提示等待结算确认。
   - 到期日已经过去：才允许生成到期作废建议。
   - 若股票数量变化能够精确证明交割，仍允许在到期日前识别提前指派/行权；这条证据链不受上述日期门槛影响。

## 验证

- 新增“到期日前消失”和“到期日当天消失”回归，均确认 `suggestion=null`，不会出现“到期作废”补录按钮。
- 新增“股票交割精确匹配时允许提前指派”回归，确保没有误伤合法提前指派流程。
- 新增“不可信空 TWS 快照不得产生任何对账建议”页面回归。
- 更新前端资源版本，避免浏览器继续命中旧脚本。
- JavaScript 全量：**908 passed，0 failed**。
- Cost Basis Python 定向：**164 tests OK，1 skipped**。

---

# 从现金流到最终交割结果的全链路复核（2026-08-26）

## 总结

期权开仓/平仓净现金、部分平仓按比例释放权利金，以及短 Put、短 Call、长 Call、长 Put 四种指派/行权的方向和交割金额公式本身正确。当前遗留错误主要发生在进入计算核心之前：程序会丢弃报表已经提供的期初成本证据、把平仓误当成开仓，或者仅凭 TWS 的期末数量差异猜测曾经发生的事件。

本轮不是只用合成用例。已逐行核对 `Reports/U********/U********_20260803_20260825.csv` 的 Trades、Open Positions、Basis、Proceeds 和 Commission，并用同一报表重放当前导入器和计算核心。

## P1：真实 SLV 平仓被覆盖式重建为七个新空头，且报表已有的期初成本被丢弃

8/25 报表中有七笔 SLV 期权交易，数量均为负、IBKR Code 均含 `C`，含义是卖出平掉报表期初已有的多头。期末 SLV 已经归零，所以 Open Positions 段没有 SLV 行。

当前导入器只有在 Open Positions 中找到“所选标的”的行才把期末持仓视为有效。于是“完整期末持仓段存在，但 SLV 因已归零而没有行”被误读成“没有期末持仓资料”；Code `C` 又没有保留为必须平仓的约束，七笔卖出全部被普通 `option_trade` 当成新开空仓。

真实重放结果：

| 项目 | IBKR 报表证据 | 当前程序 |
| --- | ---: | ---: |
| 平仓行数 | 7 | 7 |
| 期末未平仓 SLV 期权 | 0 | 7 个空头合约组 |
| 平仓净回款 | +6,509.370012 | +6,509.370012 |
| 被平仓旧仓的 `Basis` | -33,712.182800 | 0（整列未映射） |
| 报表可证明的累计结果 | -27,202.812788 | +6,509.370012 |
| 导入问题 | 应识别期初仓/成本 | 0 条问题，允许确认导入 |

`Basis + 平仓净回款` 与报表逐合约的 Realized P/L 完全闭合。这些行没有洗售等调整码，因此文件已经提供了重建所需的期初成本证据。导入器却把中文 `基础` 列列入 `unmappedColumns`，页面也不显示未映射列，最后生成价格和现金均为 0 的 `prior_open`，甚至在 SLV 归零场景连补桩也没有生成。

该问题同样适用于：

- 报表期初持有股票、期内全部卖出、期末归零；
- 报表期初持有 FUT、期内全部平仓、期末归零；
- FOP/OPT 期初仓在报表期内平仓、到期或交割。

修复要求：

1. 区分“Open Positions 段不存在”和“完整段存在但所选标的没有行”；后一种应把所选标的期末数量认定为 0。
2. 映射并保存 IBKR `Basis` / `Realized P/L`，对干净的期初平仓逐行做 `basis + close cash = realized P/L` 守恒校验；有洗售/调整码时阻断或显式降级，不能猜。
3. 保存 IBKR `O/C` 意图。`C` 行必须由反向的已有仓位支撑，不得穿过 0 变成新开仓；`O` 行也不能被旧仓自动吞成平仓。
4. 覆盖式重建必须用报表独立证明“期初 + 期间变动 = 期末”；证明不了就阻断，不能以净现金恰好相同为理由放行。

## P1：TWS 差异匹配会生成方向错误或任意行权价的“高置信”交割

当前对账按合约键排序后贪心消耗股票数量差。它没有先证明 TWS 期权数量确实是向 0 收敛，也没有证明多个候选行权价中只有一个解。

已复现两个反例：

1. 账本短 Put `-1`、TWS 短 Put `-2`、股票差 `+100`。程序生成“高置信短 Put 指派”，但草稿 `contracts=-1` 会把短仓从 `-1` 增加到 `-2`，方向正好相反。SQLite 最后会拒绝该草稿，因此暂未造成数据库污染，但页面建议本身错误。
2. 两个已消失的短 Put，行权价分别为 69 和 70，各一张；TWS 多出 100 股。两种“一个被指派、另一个到期”的解释都成立。程序按键排序任意把 P69 记为指派（现金 -6,900），把 P70 记为到期，并把两条都标成高置信。若真实被指派的是 P70，用户确认后综合成本永久少 100，存储层无法发现，因为每条事件单独看都合法。

更根本地说，即使只有一个候选，TWS 当前持仓也不能证明那 100 股不是普通股票成交；同理，“到期日已过 + TWS 无合约 + 股票不变”也不能区分零现金到期与此前已经用非零现金平仓。TWS 快照只能发现差异，不能证明历史现金流。

修复要求：只对“同方向向 0 收敛且不穿零”的数量变化考虑关闭；按账户/交割方向做全局唯一匹配，存在多解时不得生成事件；到期必须来自 CSV `Ep`、成交/结算证据或用户明确选择，不能仅凭 TWS 缺失标成高置信。

## P1：FOP 被交割成 FUT 后，当前自然操作路径会留下半本账

FUT 账本的 TWS 对账只是分别列 FUT 和 FOP 数量，没有把“消失的 FOP”与“新增的 FUT”配成一次交割。

已复现：账本短 FOP Put `-1`，TWS 中 FOP 已消失、对应 FUT `+1`。页面显示：

- FUT：`tws_only`，提供“采信 TWS”；
- FOP：`ledger_only`，没有交割建议。

点击“采信 TWS”只会新增 FUT 基线，不会平掉 FOP。结果是 FUT 持仓存在，但原 FOP 权利金仍被算作未平仓、仍在风险中，交割链和综合成本同时错误。

修复要求：提供 FUT 采信按钮前，先检查是否存在可唯一解释该 FUT 的消失 FOP；能唯一配对时应生成一条 FOP assignment/exercise（交割 FUT 的成本为 strike、现金仅手续费），不能采信独立 FUT 基线；多解时阻断并要求 CSV 或人工核实。

## P1：未知 `prior_open` 成本只在导入预览出现一次，入库后不再持续告警

`prior_open` 事件以 0 权利金入库后，计算核心不产生任何“成本不完整”告警；流水表也不显示 tag/note。清除导入预览或重新打开页面后，头条成本和“累计已实现权利金”看起来是完整结果。

SLV 顺序导入 8/21、8/24、8/25 可以把七个合约正确归零，但七条期初开仓仍是 0 成本；页面会把 +6,509.370012 全部显示为已实现权利金，而报表 `Basis` 证明旧仓成本是 -33,712.182800。

修复要求：任何有效 `prior_open` 都必须在总览持续显示 P1 级“成本不完整”，并禁止把相关 headline 称为实际综合成本；导入更早报表或用已核验 Basis 替代这些桩后才能消除告警。

## P2：FUT/FOP 结构键把同一合约月拆成两个持仓

`futureKey` 使用原始 `futureExpiry` 的前 8 位。手工表单只能保存 `YYYYMM`，IB/TWS 常返回 `YYYYMMDD`。即使 Con ID 完全相同，`202609` 与 `20260918` 仍成为两个键，对账显示一个 `ledger_only` 加一个 `tws_only`。

修复要求：结构键统一到合约月 `YYYYMM`，再用 Con ID / localSymbol 做真实合约身份消歧；精确最后交易日可另存审计字段，不能参与月份键的两种格式分裂。

## P2：SQLite 时间线仍按日期和插入顺序，不按券商秒级时间

浏览器计算会从 note 恢复 `brokerTimestamp` 并按秒排序，但 SQLite 的期权和 FUT 时间线校验仍使用 `ORDER BY trade_date, seq`。同日较早的成交在以后批次补入时，`seq` 反而更大；计算核心和入库校验看到的是两条不同时间线，可能接受实际发生在开仓之前的关闭，也可能拒绝正确的回补。

修复要求：把 broker timestamp 作为正式列持久化，存储校验和前端计算使用同一排序规则；只有确实没有秒级证据时才回退到 `trade_date + seq` 并保持歧义提示。

## P2：TWS 快照在 WebSocket 断线和重新拉取期间可能继续被当成当前状态

页面收到一次可信快照后，WebSocket `onclose` 不会清空 `positionsConnected`、快照时间或 reconciliation。下一次 `requestPositions()` 也不建立请求代次、不清空旧 `positionsAt`；新请求超时后旧表仍可留在页面。保存快照时只检查曾经有过 `positionsAt`，可能把断线前旧持仓写成当前 TWS 旁证。

修复要求：后端连接或 IB 连接断开时立即使快照失效并重算；每次拉取使用 generation/request id，只接受本次完成响应；保存快照只能带入当前连接代次内的可信完整持仓。

## P2：FUT 汇总漏掉结算/手工调整现金

单账户 FUT 的 `lifetimeNetCash` 包含 `futuresSettlementCash`，多账户合并行却没有加它。已复现：FUT 已平仓后有一笔 `manual_adjust cash=-100`，账户行显示 -100，合并行显示 0；`netCash` 本身仍为 -100。

修复要求：合并汇总直接累加各账户 `lifetimeNetCash`，或完整包含 settlement/manual cash，并加“无 FUT 持仓 + 手工结算现金”的回归。

## P2：`manual_adjust` 表单允许填数量，但计算核心静默忽略

页面声明 `manual_adjust` 可填写 `shares` 和 `price`，存储层也接受；计算核心遇到该类型立即返回，只计 `cashAmount`，完全不移动股数。已复现：`shares=100, price=50, cash=-5000` 入账后显示股数 0、综合成本为空。

修复要求：若它是纯现金调整，就从 UI 和数据校验中禁止数量/价格；若要支持期初数量修正，就定义明确的持仓和成本语义并实现，不能接受后静默丢弃。

## P2：计划要求的 Corporate Actions/拆股 CSV 导入尚未实现

导入器没有 Corporate Actions section alias，也没有生成 split 待确认草稿。跨拆股期间的报表会直接忽略该段；股票数量、期初推导和调整期权的 multiplier 都可能错。

修复要求：至少识别相关拆股并阻断导入，直到用户确认 split 和调整后合约身份；不能把含相关 corporate action 的文件当成普通完整报表放行。

## P3：负价格 FUT 被静默改成正数

导入器对 FUT 价格使用 `Math.abs(price)`，存储层拒绝负 `price/rollToPrice`。负价格期货虽罕见但真实发生过；当前行为会把经济方向改写，而不是明确说明不支持。

修复要求：允许有符号 FUT 价格并验证公式，或在导入预览阻断并明确提示；不得取绝对值后继续计算。

## 已确认正确的部分

- TQQQ 8/25 报表没有 `prior_open`。18 条原始 Trades 行合并为 16 个账本事件后，原始 `Proceeds + Commission = -13,009.650920`，事件现金为 `-13,009.650921`，仅有百万分位舍入差。
- TQQQ 最终持股 200，已实现权利金 457.542332，未实现权利金 632.806747，保守综合成本 68.212288，假设未平仓期权全部归零为 65.048255；这条真实数据路径没有发现现金或交割错误。
- 四种 OPT 指派/行权的交割方向、`shares = contracts × multiplier` 和“交割现金只含股票 strike 交割、不重复期权费”均被存储层严格校验。
- FOP 的交割事件模型本身正确：交割 FUT 以 strike 进入持仓，事件现金只含手续费；FUT roll 的价差会进入等效成本。当前问题是 CSV/TWS 身份、匹配和汇总路径，不是这两个公式。

## 为什么现有测试仍全部通过

现有回归覆盖了“所选标的在 Open Positions 中仍有行”的期初推导、普通 O/C 开平仓、交割配对和重复 CSV，但没有覆盖：

- 所选标的期末恰好归零，因此 Open Positions 完全没有该标的行；
- Code `C` 必须关闭期初仓且不能反向开仓；
- 使用 Activity Statement 的 `Basis` 重建期初现金；
- 多个行权价竞争同一股票差额；
- TWS 期权数量增加或穿零时禁止交割建议；
- FOP 消失与 FUT 新增的交割联动；
- `YYYYMM` 与 `YYYYMMDD` 的同一 FUT 身份；
- FUT 合并汇总中的 settlement/manual cash。

因此“测试全绿”只能说明既有场景没有回归，不能证明当前账本满足完整现金流与最终交割守恒。


---

# 全链路问题最终修复与验收（2026-08-26）

## 结论

上一节列出的所有已知代码问题均已修复并建立回归门禁。当前没有仍在静默
产生错误现金、错误持仓或错误交割事件的已知路径。无法从输入证据唯一确定
成本或历史事件的场景现在一律阻断或只显示差异，不再猜测。

## 逐项落地状态

| 上一节问题 | 最终处理 |
| --- | --- |
| **SLV 期末归零被重建为新空头** | Open Positions 段存在即视为完整期末清单，所选标的无行表示期末为 0；Activity Statement 的 `Basis`、`Realized P/L` 和 O/C 码已映射。`C` 必须关闭既有仓位，`O` 不得被旧仓吞成平仓。干净的完整平仓用 `Basis + close cash = realized P/L` 还原期初现金，洗售或混合批次阻断。真实 SLV 8/25 最终 0 股、0 期权、累计结果 **-27,202.812788**。 |
| **TWS 差异猜错交割或任意行权价** | 删除所有从持仓快照制造 assignment / exercise / expiry / share trade 的路径。数量增加、反向、多个候选、疑似交割和已过到期日都只给说明；真实事件必须来自 CSV 或手工核实。 |
| **FOP 消失、FUT 新增后只采信半本账** | 若消失 FOP 的交割方向和数量可能解释新增 FUT，FUT 行被标记 `adoptionBlocked`，不能单独“采信 TWS”；要求完整 CSV 或一次性手工补全交割。不会再留下 FUT 已有而 FOP 权利金仍未实现的半条链。 |
| **未知 `prior_open` 告警只出现一次** | 计算核心对每一条有效 `prior_open` 持续产生 `unknown_prior_open` 告警；重新打开页面后仍显示“非完整实际综合成本”。由 IBKR Basis 安全还原的行改用 `prior_basis`，不再误报未知成本。 |
| **FUT `YYYYMM` / `YYYYMMDD` 分裂** | FUT 结构身份统一到前 6 位合约月；精确日期仍保留作审计字段，Con ID/localSymbol 继续负责真实身份消歧。 |
| **SQLite 与浏览器日内顺序不同** | schema 升级为 v4，`broker_timestamp` 成为正式列；导入、序列化、快照哈希、列表、插入校验和冲销重放全部使用“券商秒级时间，缺失才回退当日末 + seq”的同一规则。 |
| **断线/重拉期间沿用旧 TWS 快照** | WebSocket 关闭和每次刷新均立即清空快照、均价、市场价和对账；请求带 generation/request id，只接受最新直接响应。`ibConnected` 与 `positionsReady` 必须都明确为 `true`，字段缺失也不再把空数组当成零持仓。保存快照只附带当前连接代次的可信 TWS 数据。 |
| **多账户 FUT 合计漏 settlement/manual cash** | 合并值直接累加每个账户的 `lifetimeNetCash`；无 FUT 持仓但有手工结算现金的多账户回归已覆盖。 |
| **`manual_adjust` 接受数量后静默忽略** | 明确定义为纯现金调整：页面不再显示数量/价格字段，存储层拒绝 shares、contracts、FUT 数量、价格及合约字段，并强制填写说明。 |
| **Corporate Actions 被忽略** | 已识别中英文 Corporate Actions section。只要与当前标的相关即整批阻断，要求先确认 split/调整合约身份；不相关标的不会误伤。 |
| **负价格 FUT 被改成正数** | 导入器保留有符号 FUT 价格，SQLite 接受负 `price` / `rollToPrice`，普通 FUT 和 ROLL 均有回归。 |

## 本轮真实报表中新发现并修复的问题

IBKR 对行权/指派的期权腿会把 `Basis` 转入交割标的，因而该期权行常显示
`close proceeds = 0`、`Realized P/L = 0`，但 `Basis` 仍是精确的历史开仓现金。
此前把普通平仓的守恒式机械套到这种行上，会把 SPY 四个已指派合约的有效
权利金证据误判为不可用。

现在仅在同时满足“assignment/exercise、零期权平仓现金、零期权 realized P/L、
无 W 调整码、数量完整闭合”时采信该 Basis。真实 8/21、8/24、8/25 SPY
文件中四个指派期初仓均从 `prior_open` 升级为 `prior_basis`；合成测试也锁定
了这条 IBKR 特殊语义。

## 真实 Reports 时序验收

新增 `tests/cost_basis_reports.test.js`，直接读取 `Reports/U********/` 的三份
真实文件，执行 8/21 → 8/24 → 8/25 重叠累计导入，并与空账本直接使用 8/25
覆盖式重建比较：

- GLD、QQQ、SLV、TQQQ、USO 的股数、未平期权数、净现金、已实现权利金、
  未实现权利金和告警逐项相同。
- TQQQ：200 股，净现金 **-13,009.650921**，已实现权利金 **457.542332**，
  未实现权利金 **632.806747**，6 个未平期权，零告警。
- SLV：0 股、0 个未平期权，累计已实现/净现金均为
  **-27,202.812788**，零告警。

SPY 没有被强行做成绿色：三份报表都从 8/3 开始，而
`SPY 17SEP27 P605` 同时包含报表期前旧仓和期内新增仓，期末 Position Basis
不能唯一拆分到两批。程序现在稳定地只报告这一项阻断；其余 23 个 SPY
期初合约均由 Basis 安全还原。要得到 SPY 的完整实际成本，必须提供覆盖该
旧仓开仓的更早报表或人工核实这一个期初成本。这是输入证据不足，不是待修的
静默计算 bug。

## 自动化门禁

- JavaScript 全量：**930 passed, 0 failed**。
- Cost Basis 存储/WebSocket 定向：**174 tests OK, 1 skipped**；跳过项是可选
  IB 桥接环境门禁。
- 安装 `requirements-ib-bridge.txt` 到隔离临时环境后：
  `ib_server_ws_test` **58 passed**，`ibkr_adapter_pricing_test` **105 passed**。
- Python 全量（含 IB 依赖）：**794 tests OK**。
- 资源版本检查与 `git diff --check`：通过。

## 当前仍需外部样本验证的边界

`Reports/` 仍没有真实 FOP 指派、FUT 成交和 FUT ROLL 行。因此这些路径已经
通过公式、合成 IBKR 格式、SQLite 不变量和对账行为测试，但仍不能宣称覆盖了
券商所有真实本地化字段组合。当前实现对此保持 fail-closed；真实 FOP/FUT
文件到位后应再增加与本次 OPT/STK 相同级别的文件驱动回归。


---

# 对「全链路最终修复」的审查（2026-08-26）

独立复核，不采信修复记录本身：每一项都重新复现原缺陷、再验证修复。

## 记录中的数字全部独立复算通过

不跑对方的 `cost_basis_reports.test.js`，用自己的重放脚本读 `Reports/U********/` 三份真实文件：

| 标的 | 股数 | 净现金 | 已实现权利金 | 未实现权利金 | 未平仓 | 累计导入 == 覆盖式重建 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| TQQQ | 200 | -13,009.650921 | 457.542332 | 632.806747 | 6 | ✅ |
| SLV | 0 | -27,202.812788 | -27,202.812788 | 0 | 0 | ✅ |
| GLD / QQQ / USO | 0 | — | — | — | 13 / 12 / 2 | ✅ |
| SPY | 0 | — | — | — | 27 | ✅（恒定阻断 1 项） |

与记录逐位一致。SPY 稳定只报一项阻断，符合「输入证据不足、不是静默 bug」的说法。

## 逐项复现验证

| 修复项 | 复现结果 |
| --- | --- |
| TWS 差异不再猜交割 | 短 Put 消失 + 股票 +100 → **0 条草稿** ✅ |
| 审计反例 1（TWS 短仓变多） | 账本 -1 / TWS -2 / 股票 +100 → `quantity_mismatch`，无草稿 ✅ |
| 审计反例 2（两个候选行权价） | P69/P70 各一张消失 + 股票 +100 → **0 条草稿** ✅ |
| `prior_open` 持续告警 | 产生 `unknown_prior_open`；`prior_basis` 不误报 ✅ |
| `manual_adjust` 拒绝数量 | 带 shares/price 被拒；缺说明被拒 ✅ |
| 负价格 FUT | -37.63 原样入库，符号保留 ✅ |
| FUT 合约月归一 | `202609` 开仓 + `20260918` 平仓被接受为同一合约 ✅ |
| Corporate Actions | 本标的拆股整批阻断；其他标的不误伤 ✅ |
| 前后端时序一致 | 两侧同为「日期 → 券商秒级时间 → seq」，回退串一致 ✅ |
| schema v3 → v4 迁移 | **在用户真实库副本上演练**：16 条事件、净现金一分不差，`broker_timestamp` 列就位 ✅ |

## 审查中发现并已修复的遗留

### `prior_open` 的「成本不完整」没有落到头条数字上

修复记录要求「**禁止把相关 headline 称为实际综合成本**」，但实现只把 `unknown_prior_open` 放进告警列表；`summarizeCost` 不返回任何不完整标志，`_headlineRow` 也不据此改写。页面顶部仍以「68.2123」这类确定数字呈现，告警在下方另一个区块——扫一眼页面的人看到的是一个自信的结论。

实测 SPY（真实数据、含 1 个未知期初桩）：头条 `costIncomplete` 为 `undefined`。

已修复：

1. 两条 fold 路径（股票/期权与 FUT）都把 `prior_open` 标记为账户级 `costIncomplete`；两条合并路径都继承。
2. `summarizeCost` 的**每一条返回路径**都返回布尔 `costIncomplete`。原先「无持股」分支提前返回，而 SPY 与 SLV 恰好都是 0 股——最需要这个标记的场景反而拿不到它。
3. 头条单元格追加「（成本不完整）」并使用告警色；无持股时显示的累计已实现同样标注——一本已清仓的账，其累计数字与每股成本一样会被空权利金桩污染。

真实数据复验：SPY 标记为 `true`，TQQQ/SLV/GLD/QQQ/USO 全为 `false`，与各自的未知期初桩数量一一对应。

## 门禁

- JavaScript：930 → **935 passed, 0 failed**（新增 4 项：头条标记、`prior_basis` 不误报、已清仓账本仍标记、每条返回路径都给布尔值）。
- Python 全量：**633 tests**，除两个环境性 `ModuleNotFoundError: ib_async` 外全部通过。
- 资源版本检查通过。

## 给用户的提示

用户当前真实库为 **schema v3、16 条事件**，净现金 `-13,009.650054`；当前导入器对同一份 8/25 报表算出的是 `-13,009.650921`，相差 0.00087。差异来自更早版本的导入器精度，不影响每股成本（约 0.0000043/股），但如果要与报表逐位对齐，可用覆盖式重建按 8/25 文件重建一次。首次打开页面时 v3 → v4 迁移会自动执行，已在真实库副本上验证无损。


---

# 多账户账本与删除账本的审查（2026-08-26）

审查对象：账本身份加入 IB 账户（schema v5）与 `delete_cost_basis_book`。

## 结论：两项都实现正确，**本轮未发现缺陷**

三处我一度判定为缺陷的，复查后全部是我自己的测试绕过了真实调用链，已在过程中逐一更正：

1. 对账把其他账户的持仓报成差异 —— 页面调用前用 `_positionsForBook(book)` 按账本账户过滤过；走真实链路时 `balanced = true`，遗留账本才保留全账户视图。
2. 导入不校验报表账户 —— 早已由 `targetAccount` 实现（我 grep 的关键词没覆盖到它，测试又传了不存在的 `bookAccount`）。我据此加的重复逻辑已撤销，工作区恢复原状。
3. 对账合约两侧键不匹配 —— 上一轮的 harness 给 TWS 快照编了与账本冲突的 conId。

## 逐项验证

### schema v4 → v5 迁移

在**用户真实库的副本**上演练：数据无损，账户 `U********` 被正确采纳为账本身份。迁移逻辑只在「全部非空事件账户一致且无歧义空账户行（书级 split 除外）」时采纳账户，否则留作遗留混账户账本——不移动、不改写任何事件。

### 多账户

| 场景 | 结果 |
| --- | --- |
| 同标的、两个账户 | 允许两本账 ✅ |
| 同账户、同标的重复 | 拒绝 ✅ |
| 写入其他账户的事件 | 拒绝 ✅ |
| 写入空账户事件 | 继承账本账户，不留空 ✅ |
| 遗留账本（account=""） | 仍接受多账户事件，向后兼容 ✅ |
| 导入其他账户的报表 | 预览即阻断 ✅ |
| 报表账户无法确定 | 预览即阻断 ✅ |
| 对账 | 按账本账户过滤 TWS 持仓 ✅ |
| 遗留账本的呈现 | 选择器、元信息、录入提示、两处删除对话框统一显示「旧版未限定账户」 ✅ |

### 删除账本

| 场景 | 结果 |
| --- | --- |
| 短语 | `DELETE <账户> <标的> N EVENTS M SNAPSHOTS K RESETS`，遗留账本省略账户 |
| 错误短语 | 拒绝 ✅ |
| 篡改计数 | 拒绝 ✅ |
| **过期计划**（取计划后又追加事件） | 拒绝 ✅ —— 在写锁内重算，无法删除操作者没看过的记录 |
| 正确删除 | 账本、事件、快照、重建存档一并删除 |
| 其他账户的账本 | 不受影响 ✅ |
| 页面流程 | 取计划 → 防止中途切换账本 → 展示全部明细 → 短语确认 → 服务端复核 → 清空全部页面状态 |

删除**刻意不留存档**（语义是彻底移除，不是可恢复的清空），与 reset/rebuild 区分明确。

## 一个次要不一致（已处理）

`delete_book` 接收并校验 `client_token`，但刻意不保存删除回执：否则“彻底删除”后仍会留下账本身份记录。原实现中响应丢失后的重试会返回 `book_not_found`，页面却显示普通删除失败。

现已处理：页面把 `book_not_found` 解释为「账本已不存在，可能上一次删除已成功」并刷新权威账本列表；删除请求已发出后若超时或断线，也会在连接仍可用时重新读取列表，区分“账本仍存在”和“已确认不存在”。如果删除成功响应已经收到、只是列表刷新失败，仍明确报告删除成功；如果连删除结果和列表都无法读取，则显示“结果暂无法确认，连接恢复后以账本列表为准”，不再把未知结果说成失败。数据库仍不保留删除回执或账本残留信息。

## 门禁

- JavaScript：**939 passed, 0 failed**
- Cost Basis 存储 / WebSocket：**185 tests OK, 1 skipped**
- 新功能测试覆盖：存储层 7 项、协议层 5 项
- 资源版本检查通过；工作区已恢复到审查开始时的状态（我的重复逻辑已撤销）


---

# 删除幂等提示修复的查验（2026-08-26）

审查上一节提出的「`delete_book` 非幂等、重试时提示误导」的修复。

## 结论：修复正确，且比建议的更周全

修复没有停在「把 `book_not_found` 改个说法」，而是把删除的失败态拆成六种分别处理。其中一条是我没有提出的真实失败模式：**发出销毁性请求后超时或断线并不等于删除失败**，响应可能只是丢了。代码在这种情况下重读权威账本列表，再根据账本是否还在给出结论。

## 浏览器实测（真实页面 + 真实存储层生成的响应）

六条分支逐一驱动，全部符合预期，全程零 JS 错误：

| 分支 | 实际提示 |
| --- | --- |
| 本地短语不匹配 | 确认短语不匹配，账本未被删除。 |
| 服务端 `delete_confirmation_mismatch` | 删除计划已变化或确认短语不匹配；账本未被删除，请重新操作。 |
| `book_not_found`（上次已成功） | 账本已不存在，可能上一次删除请求已经成功；账本列表已刷新。 |
| 正常删除 | 已永久删除 U******** · TQQQ：16 条事件、0 个快照、0 份重建存档。 |
| 删除成功但刷新列表失败 | 账本已删除成功，但刷新账本列表失败：busy。连接恢复后会自动刷新。 |
| **超时但服务端已删除** | 删除响应虽然丢失，但刷新后确认账本已不存在，删除已经成功。 |
| **超时且服务端未删除** | 删除请求未得到确认；刷新后账本仍存在，因此没有删除。 |

最后两条最关键：同样是 20 秒超时，页面给出的结论相反且都正确——它不猜，而是以账本列表为准。

`deleteSubmitted` / `deleteConfirmed` 都是每次调用内声明的局部变量，不会跨调用泄漏。

## 门禁

- JavaScript：**939 passed, 0 failed**（两次连跑一致）
- Python 全量：**644 tests**，除两个环境性 `ModuleNotFoundError: ib_async` 外全部通过
- 真实报表回归：TQQQ 净现金 `-13,009.650921`、已实现 `457.542332`；SLV 归零 `-27,202.812788`；SPY 恒定阻断 1 项——与本轮改动前逐位一致
- 资源版本检查、`git diff --check` 通过
