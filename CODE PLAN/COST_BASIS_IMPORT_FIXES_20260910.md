# Cost Basis 剩余导入问题修复记录

2026-09-10。范围：`COST_BASIS_IMPORT_FIX_VERIFICATION_20260910.md` 的 V01–V12，以及其中关联的 R01–R20 剩余验收项。原 review 与复现日志保留，作为修复前的历史证据。

## 已实施

| 验收项 | 修复结果 | 回归证据 |
|---|---|---|
| V01 同引用修订 | 页面在标记已存在前比较经济项；服务端补齐换月目的合约和可用券商身份检查，作废/排除引用也拒绝静默跳过 | pipeline：同 ID 改价格/数量/费用；store 原子拒绝 |
| V02 直接 execId | 直接命中也比较经济项，重复认领或修订不能显示已核对 | pipeline：变更 ID 对应数量/价格被阻断，完全相同的重放通过 |
| V03 跨格式/数量/无时间 | 已有 CSV 每批一对一认领；数量修订及缺时间重叠阻断；现金行不同引用的同日重叠同样需核对 | pipeline：1 对 2、数量修订、日期精度不足、现金修订 |
| V04 Flex 存根 | 账户统一传递，按新增历史净开平仓数量证明替换；服务端重证，同事务冲销和写入 | pipeline→SQLite：Flex/Activity、净 -2+1 补录，最终均短 1 张 |
| V05 手工交割 | 指派/行权/到期参与重复检查，期货换月也进入种类对应的数量与合约核对 | pipeline：有剩余短仓的手工指派重复被阻断 |
| V06 覆盖归属 | schema v9 存档覆盖登记；重建仅显示新历史证明，恢复同时恢复对应登记；历史修改使旧证明失效但保留审计记录 | SQLite：重建、恢复、冲销、v8 升级及损坏归档拒绝；async：未核实月份不合并 |
| V07 零成交月份 | 无 Trades 也核对 Period 内未认领 TWS；缺必要检查的导入不计入完整覆盖 | pipeline：TWS 一开一平净仓为零仍阻断 |
| V08 分页版本 | 行数据与 digest 在同一读事务取得；浏览器只接受同版本、完整分页 | async：旧 1000 行 + 新 1 行被拒绝，不发布可用账本版本 |
| V09 强制凭据 | 导入、重建、旧 reset、存档恢复及文件恢复要求完整身份和版本；保留幂等重试 | store 与原始 WS：缺身份/缺版本/部分身份均拒绝；同条数旧版本拒绝 |
| V10 日期/坏合约 | 零成交首份报表按 Period 起点前一天生成起点；无日期证据时预览阻断；本标的坏期末合约不能读成零 | pipeline→SQLite：100 股/5000 成本正确；坏期权名拒绝 |
| V11 Order/Trade | 子行经主行同等校验；核对账户、合约、币种、总数量、带符号佣金、净现金及打印精度内均价 | pipeline：金额矛盾拒绝；正常补余量得到 20 股/-1002 |
| V12 用户闭环 | 重建提示检查前后端缩水；FUT 仓位进入预览；新增后端一致性 JSON 导出与显式恢复，校验摘要、身份和版本，先存档再替换；修正文案 | pipeline：FUT 当前/新增后/报表期末；store/WS：备份往返、保留作废状态、损坏拒绝、loopback |

同时修正了提交处理器自身的问题门槛、重建计划迟到/版本变更保护、提交期间换文件/账本及迟到完成回调，避免仅依赖按钮禁用状态。

## 验证结果

- JavaScript 全量：**1154 通过，0 失败**。
- 项目配置 Python 环境：账本专项 **249 通过**；`ib_server_ws_test.py` **75 通过**，合计 **324 通过**。
- `git diff --check` 通过；页面资源内容哈希已更新。
- 新增：`tests/cost_basis_import_pipeline.test.js`、`tests/cost_basis_import_pipeline_test.py`、`tests/cost_basis_import_async.test.js`；增加备份与缺凭据协议回归。
- 既有行为测试更新为携带新协议所需身份/版本的模拟客户端；强制凭据的拒绝测试使用原始 store/WS，不经过这些测试辅助器。
- 全部数据写入验证发生在临时数据库。未连接真实券商，未修改真实账本，也未启动真实数据库迁移。

## 生效与范围

后端需重新加载代码，页面需刷新。数据库在后端下次初始化时升级到 v9，原交易事件保留；v8 无法追溯到具体重建历史的覆盖登记标为待重核，需要再次导入相应报表建立证明。

相同券商 ID 的修订采用明确阻断与完整累计报表重建；没有实现任意逐笔修订替换。误冲销只有在存在合适存档/备份时才能恢复，否则需重新核实并重建。新 JSON 恢复仅接受带摘要的 v1 文件，页面上限 7 MiB；文件恢复后重新核对报表覆盖。大型数据库使用 README 所述 SQLite 一致性备份流程。

这些测试覆盖已知反例和相应正常路径，不宣称穷尽所有券商报表模板。具体当前行为见 `COST_BASIS_IMPORT_INTEGRITY.md`。
