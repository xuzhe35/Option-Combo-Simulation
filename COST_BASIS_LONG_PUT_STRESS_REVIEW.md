# COST_BASIS 压力测试维护主线

> 2026-09-05：压力测试 v2 内核重构。本文只保留现行边界、估值口径和维护入口。
> 原先“所有精度问题均关闭”的结论已撤回：计算不变量可以验证，预测精度和真实行情质量仍需独立检验。

## 1. 保留与替换的边界

账本、导入、对账、数据库结构与 What If 交割预览保留。账本仍是仓位/现金流真相，TWS 只补充快照和检测差异，压力测试绝不自动写事件或下单。

已移除页面中的旧压力扫点、分离的本账本/联动估值路径，替换为：

| 模块 | 职责 |
| --- | --- |
| `cost_basis_stress_models.js` | 美式/欧式定价、身份匹配、映射、参数函数 |
| `cost_basis_stress_core.js` | 一次账本编译、时间与报价验证、本地 IV 反解、统一头寸估值和分解 |
| `cost_basis_stress_band.js` | 对完整组合采样并取范围 |
| `cost_basis_stress_worker.js` | 后台执行、与页面 generation 协作 |
| `cost_basis.js` | 请求、控件、图表，不再包含定价真相 |

## 2. 盈亏恒等式

成本盈亏从现金与资产直接求和，**不得由每股综合成本反推**：

```text
本账本盈亏 = 历史净现金 + 情景股票价值 + 期权/交割价值
```

分项时，未平期权的开仓净现金从基础现金移到对应期权腿；它只出现一次，到期也不会丢失。费用已经包含在账本 cashAmount 中，不能再扣一次。零股票不意味着没有组合价值。

两种压力口径：

- **现金流成本盈亏**：本账本含历史现金流；联动多头仍显示相对当前快照的变化。这是明确标注的混合口径，不是账户 ΔNAV。
- **相对快照变化**：本账本股票/期权与联动多头都以当前快照为基线；同刻零冲击应收敛到零。

右轴显示**情景结算后每股成本**：复用账本引擎，按情景路径逐到期交割/归零后计算所选净现金、股票均价或税务口径；不再误用当前成本常数。净现金成本仍排除 Long Option 权利金全生命周期，但本账本 Long Option 行权的股票交割会改变股数和股票成本。无股票或历史成本不完整时成本不显示；盈亏仍独立计算，不由成本反推。联动保护市值、未到期期权估值与每周假设净收入不进入此成本线。

虚拟交割只存在内存中，按截止时间排序，相同交割组合的成本缓存复用；IV 范围成员不会反复重放历史。成本在相同交割结果的价格区间内可以水平，跨交割边界会跳变。三种口径、跨到期路径、零持股及成本/盈亏解耦均有回归测试。

默认计入所有存续本账本期权；关闭时排除更晚期权及其未平权利金，并明确提示“部分组合”。额外每周输入是**净收入假设**，按实际经过天数/7列示，不表示新增卖方期权一定盈利。

## 3. 时点与交割

- 带时区的参考快照、情景时刻、合约截止时间分开处理。N 天为快照后 N×24 小时，0 天就是同一时刻。
- 合约精确 `expiryAsOf` 优先；缺失时明确使用纽约 16:00 收盘假设，涵盖 DST 转换但不能替代特殊合约/半日市 metadata。
- 已过截止时间却仍在账本中的期权要求先对账，不能按“未来情景”重新发明已发生的到期结果。
- 本账本 ITM 到期按路径交割股票，交割股票继续持有到目标时刻；联动多头在其到期时变现并持有现金，不合并联动股票或空头。
- 支持“立即冲击并保持”与“渐变至终值”；选择不同路径可能改变途中到期的行权结果。
- 美式模型计入提前行权价值，但**不模拟实际提前指派时点**。到期 ITM 自动交割也是模型假设，不是对实际指派的保证。[OIC 行权说明](https://www.optionseducation.org/optionsoverview/exercising-options)。

## 4. 行情与定价

- 当前标记价用同一所选模型、时钟、股息率和利率反解本地 IV；broker IV 保留为行情/路径 σ 代理信息，不直接当作能对齐 mark 的保证。路径 σ 留空且缺少有效 broker IV 时，可使用当前存续合约本地反解的 IV 代理，界面明示来源；不是实现波动率预测。无报价/利率或无法反解时仍不能凭空补 σ。
- 无法反解、不合理报价、强身份/条款/乘数冲突、缺价格/利率则整体停止。不存在“少一条腿就少算一条”的降级。
- USD STK 是压力模型当前支持范围；账本/导入支持其它币种的能力不变。
- 当前及未来分数期限分别从**同一冻结曲线**解析；沿用今日期限曲线形状是静态滚动假设，并非未来利率预测或远期折现比。缺失/不可用利率不是零。
- 中间价或今日 bid/mark、ask/mark 比例外推是不同变现口径；后者需要双边非交叉报价，仍不是未来可成交价。
- 后端 v2 返回曲线、snapshotId、接收时刻、行情类型及缓存中已有的合约截止时间。页面拒绝旧版本快照；更新后要重启后端。
- 接收时刻不是交易所逐笔成交时刻。跨账本/报价接收时间差超过 60 秒拒绝；冻结、延时和未知类型明确提示，不声称原子快照或绝对新鲜。
- 开启/重新开启叠加、切换联动账本、变更情景日期或手动刷新时，先载入联动事件，再同批并行获取两边行情；完成前不计算半套组合。失败一侧不得复用旧报价；过期批次、断线和关闭后的响应不得恢复叠加。同步性门禁保持不变。
- 原有 128 合约请求上限、20 行分批、共享 8 秒 deadline、BBO 宽限与 finally 取消逻辑保留。

## 5. 映射、IV 和范围

理想单次日重置目标是 `rETF = k × rIndex`，不是 `(1+rIndex)^k`。日内/一天内采用简单收益近似；任意盘中起点仍会有杠杆漂移。多日连续近似保留显式 σ 与方差损耗，不声称终值能唯一决定路径。[ProShares 每日目标](https://www.proshares.com/our-etfs/leveraged-and-inverse/tqqq)。

IV 模式保持正交：none 不变；fixed 全曲线统一点数，不套 β 因子；beta 只在真实驱动指数下跌时抬升，并可乘期限/价外 Put 因子。反向 ETF 主轴上涨不等于指数上涨。自适应 β 与期限/偏斜研究依据见 `STRESS_MODEL_RESEARCH_MEMO.md`。

范围详见 `CODE PLAN/STRESS_CALIBRATION_BAND_PLAN.md`：

- 45 个内部/边界参数格点 + 用户中线 + 可选 flat-IV；关闭维度后成员相应减少。
- 各成员计算完整组合，上下沿不得相加各腿的独立极值。
- flat-IV 同时进入上下界；任一成员无效则撤下整条范围，保留有效中线。
- **采样情景范围，非置信区间**。有限格点不保证连续极值，也不覆盖所有路径。
- 后台 Worker，完整输入缓存键，改变参数/账本/快照或关闭时取消旧任务；迟到结果不得恢复旧范围。

## 6. 验证入口

```text
node tests/run.js
node scripts/audit_cost_basis_stress.js
node scripts/benchmark_cost_basis_stress.js
python3 -m unittest tests.stress_model_validation_test tests.cost_basis_stress_snapshot_test tests.cost_basis_store_test tests.cost_basis_ws_test tests.cost_basis_executions_test tests.ib_server_ws_test
python3 scripts/stamp_asset_versions.py --check
```

使用仓库配置的 Python 环境（不要假定裸 python 可用）。内核算例覆盖现金恒等式、费用、纯期权、到期连续性、当前价格收敛、DST、已过期头寸、身份/币种/时差拒绝、路径交割、分数期限曲线、完整组合范围及 worker 失效传播。旧公式的字节断言已迁移为新语义的独立现金流/边界测试，账本、导入、对账回归保留。

## 7. 未宣称完成的验证与后续

### 切片解释层（2026-09-05）

- 保留现有价格映射及三倍 IV 联动，不再把用户选择的固定映射当成必须替换的缺陷。
- IV 控件带研究版本与来源标签；旧配置不自动改值，提供明确的“保留当前参数 / 恢复研究基准”。恢复只涉及 β 自适应、期限 30 天 / 指数 0.65、价外 Put 折扣，不覆盖价格、倍率等其他设置。
- 图下“切片核对”随悬停或键盘滑块更新，离开图后保留所选点；同时显示成本混合口径、纳入持仓较当前快照变化、flat-IV 对照及 IV 额外贡献。对照损益均排除假设收入；关闭区间也可以核对。
- 可展开查看该点上下沿实际使用的 β 倍率、期限参数、价外 Put 折扣或 flat-IV 成员；仍是采样范围而非概率区间。
- 本账本交割付款、收款、净额与股数独立展示；权利金不再次计入，联动未到期期权估值不视为交割资金，跨日期净额不代表峰值需求或保证金充足性。
- 回归覆盖两种损益口径相互核对、缺报价只撤下附加比较项、三倍冲击不变、四种多空 Call/Put 现金方向、跨到期日收付款和旧参数显式确认。固定报价的原切片中线未改变；19 张卖方 Put 的交割付款仍为 $135,600，结算后 2,700 股。

### 仍需独立验证

已用实际 TQQQ/QQQ 持仓验证成对刷新、无 broker IV 时的本地代理和跨账本叠加。tick 顺序组合、行情权限故障、实际特殊截止时间与批次清理仍需专项验证，不能由一次页面成功推断全部覆盖。不要为测试擅自重启正在交易的桥接服务。

当前范围未做样本外概率校准。现金/融资利息、未来股票分红现金流、交易费/滑点、实际提前指派、逐日跟踪误差和完整随机路径都未模拟；连续股息率只进入期权定价，不等于已计入实际分红现金。后续优先做配对日线映射验证、联合参数的样本外区间覆盖率和实际报价残差监测，而不是继续增加未经验证的复杂模型。

历史追溯可看 `9588183`、`33cb47b`、`f2f6a23`、`713ac36`。当前开发约束以 `CODE PLAN/STRESS_KERNEL_REFACTOR.md` 为准，不再将历史“已全部解决”当作预测精度证明。
